# Website Update Status

This file tracks which full-page HTML embeds in this repo are **in sync with the live Google Site**. It is the working handoff point between two tools:

- **Claude Code** edits the HTML files in this repo.
- **Claude in Chrome** (or the user, manually) pastes the updated HTML into the matching embed block on the live Google Site.

See the "Local-to-Live Sync Workflow" section of [CLAUDE.md](CLAUDE.md) for the full process. `site-snapshot/` remains the separate content archive of record — this file is purely about push status.

## How to read the table

| Column | Meaning |
|---|---|
| Local file | HTML file in the repo root |
| Live URL | The embed's page on beaver-works-assistive-tech.mit.edu |
| Last local edit | Date of the most recent commit touching the local file |
| Sync status | ✅ Synced · 🔄 Needs sync · ❓ Unknown |
| Last synced | Date someone confirmed the live embed matches the local file |

**Sync status meanings:**
- ✅ **Synced** — the live embed matches this local file as of "Last synced."
- 🔄 **Needs sync** — local file has changed since it was last pushed; live site is stale.
- ❓ **Unknown** — sync status has never been confirmed either way; treat as needs-sync until checked.

---

## Pages with a local HTML embed

The rows below were seeded on 2026-07-12 by comparing each file's last local-edit date against its `site-snapshot` entry's `snapshot_date`. Per the existing [Google Sites to HTML Embed Workflow](CLAUDE.md#google-sites-to-html-embed-workflow), `snapshot_date` should be bumped every time an embed is pushed live — so a local edit dated *after* `snapshot_date` means the live site is very likely stale. This is an inference, not a confirmed check; do one pass to verify each row, then keep it current going forward.

| Local file | Live URL | Last local edit | Sync status | Last synced | Notes |
|---|---|---|---|---|---|
| [index.html](index.html) | [/home](https://beaver-works-assistive-tech.mit.edu/home) | 2026-08-19 | 🔄 Needs sync | 2026-08-19 | **Note:** this row's "✅ Synced 2026-07-12" status was stale — a 2026-08-05 edit (commit `46c4b65`, issue #10: swapped stale "2026 Challenge has concluded" messaging for a 2026-2027 registration-open announcement) was never reflected here or confirmed pushed live. **2026-08-19 (issue #11):** added a compact season-status line above the hero, and a new "What teams and co-designers say" section (3 testimonial quotes + Levi/Izzy/Skylar co-designer story videos, pulled up from About) placed right after the hero for above-the-fold social proof. `check-styles.py` passes with zero drift. **Confirmed live 2026-08-19 (second check, after user pasted the update):** fetched the live page's rendered HTML directly — both the registration-open messaging and the "What teams and co-designers say" testimonials section are now present. **2026-08-19, registration-link simplification (later same day):** both registration CTAs (status line, interest band) were switched from the direct formstack registration URL to `/create-challenge/current-forms`, per Hosea's request to route all registration links through that single hub page instead of hardcoding the formstack slug in multiple places. Not yet pasted live. |
| [about.html](about.html) | [/create-challenge/about](https://beaver-works-assistive-tech.mit.edu/create-challenge/about) | 2026-08-19 | 🔄 Needs sync | 2026-07-12 | Added missing `body { font-size: 1rem }` (style-guide drift fix), then removed `target="_blank"` from 1 internal link (Getting Started) per the corrected link-target rule in CLAUDE.md. Rebuilt 2026-07-12 from the last git commit after an earlier truncated paste; re-pasted live and confirmed by user 2026-07-12. Re-confirmed live 2026-08-19 (fetched rendered HTML, "130 student teams" present). **2026-08-19 registration-link sweep:** two stale "notify me"-era references to the old interest-signup Google Form (`docs.google.com/forms/d/e/1FAIpQLSfjykFOIogTkRrczyDZ8J1PyDbEiwDTHsGh3cNt8zFXnhzZhw/...`) were replaced with the live 2026–2027 registration form (`mit-bwsi.formstack.com/forms/create_team_application_2026_2027`, confirmed resolving live): the "Registration opens in September!" paragraph now reads "2026–2027 registration is open!" with a "register your team" link, and the "Get notified about the start of the 2026–2027 challenge" button now reads "Register for the 2026–2027 challenge". Not yet pasted live. |
| [additional-resources.html](additional-resources.html) | [/additional-resources](https://beaver-works-assistive-tech.mit.edu/additional-resources) | 2026-07-12 | ✅ Synced | 2026-07-12 | Style-guide drift fixes (body font-size, `.section__body` margin, callout-card spacing) and a new Interest Band section added before the footer (previously had none) — copy is placeholder-quality ("Ready to put these tools to use?" linking to Getting Started and the Course); revisit wording if it doesn't read right live. Then removed `target="_blank"` from 2 internal links (Getting Started, the Course). Pasted live and confirmed by user 2026-07-12. |
| [create-course.html](create-course.html) | [/create-challenge/create-course](https://beaver-works-assistive-tech.mit.edu/create-challenge/create-course) | 2026-07-12 | ✅ Synced | 2026-07-12 | Fixed link underline/hover-opacity to match style guide, then removed `target="_blank"` from 11 internal links (module nav, maker-skills sub-links). Pasted live and confirmed by user 2026-07-12. |
| [course-introduction.html](course-introduction.html) | [/create-challenge/create-course/introduction](https://beaver-works-assistive-tech.mit.edu/create-challenge/create-course/introduction) | 2026-07-12 | ✅ Synced | 2026-07-12 | Aligned unused `.video-grid` CSS and `.video-embed` border-radius to reference values (no layout change — page uses `.video-stack`), then removed `target="_blank"` from 2 internal links. Pasted live and confirmed by user 2026-07-12. |
| [maker-skills.html](maker-skills.html) | [/create-challenge/create-course/maker-skills](https://beaver-works-assistive-tech.mit.edu/create-challenge/create-course/maker-skills) | 2026-08-15 | ✅ Synced | 2026-08-15 | Added a new "Artificial Intelligence" section card (icon, description, link to `/create-challenge/create-course/maker-skills/artificial-intelligence`) after the Wearables card. Checked live 2026-08-15: the AI card (title, body copy, icon path, link) is present and matches the local file exactly — already pasted live, this row's earlier "not yet pasted" note was stale. Re-confirmed live 2026-08-19 (link to the Artificial Intelligence sub-page still present in the rendered HTML). |
| [design-processes.html](design-processes.html) | [/create-challenge/create-course/design-processes](https://beaver-works-assistive-tech.mit.edu/create-challenge/create-course/design-processes) | 2026-08-15 | ✅ Synced | 2026-08-19 | New HTML embed (first conversion from Google Sites, merged via PR #36 — Module 2 of the CRE[AT]E Course, previously listed under "Not yet converted"; this row was missing from this table entirely until now). Checked live 2026-08-15: content (Co-Designers, The Design Process, Product Development Process Flows, What Is Prototyping, etc.) matches the local file, **but** the live page has two style-guide violations that were also present in the local file at push time: (1) the two module-nav buttons in the closing interest band had `target="_blank" rel="noreferrer noopener"` on internal links, violating the corrected internal-link rule in CLAUDE.md; (2) `.reflect-box` `margin-top` was 24px instead of the reference 32px (`check-styles.py` drift). Both fixed locally just now — `check-styles.py` passes with zero drift across all 15 pages. **Confirmed live 2026-08-19:** fetched the live page's rendered HTML directly — the module-nav links no longer have `target="_blank"`, and `.reflect-box` margin-top is 32px. Both fixes are live; this row's earlier "Not yet pasted live with these fixes" note was stale. |
| [technical-mentors.html](technical-mentors.html) | [/create-challenge/technical-mentors](https://beaver-works-assistive-tech.mit.edu/create-challenge/technical-mentors) | 2026-07-12 | ✅ Synced | 2026-07-12 | Style-guide drift fixes (body font-size, `.section__body`/`.section__list` spacing, `.interest-band__body` opacity, `.section__list` color, `.schedule-block` margin). Pasted live and confirmed by user 2026-07-12. No internal links needed the target fix — all its links are already external. **Note (found 2026-08-19, resolved same day):** this page's footer read "© 2025 MIT Beaver Works" — a year behind the "© 2026" text used on every other page — present in both the local file and the live page. **Fixed and re-confirmed 2026-08-19** (commit `e30cb84`, "fix year typo"): local file now reads "© 2026 MIT Beaver Works", and fetching the live page's rendered HTML confirms "2026 MIT Beaver Works" is live and "2025 MIT Beaver Works" is gone. Note the wording is still just "MIT Beaver Works" rather than the "MIT Beaver Works Assistive Technology" used elsewhere — a minor style inconsistency, not re-flagged as blocking. |
| [tips-for-teams.html](tips-for-teams.html) | [/create-challenge/tips-for-teams](https://beaver-works-assistive-tech.mit.edu/create-challenge/tips-for-teams) | 2026-08-19 | 🔄 Needs sync | 2026-07-12 | Style-guide drift fixes (`.section__list` spacing, `.section__ctas` margin, `.callouts`/`.callout-card` spacing). Pasted live and confirmed by user 2026-07-12. Already had no `target="_blank"` links — no further change needed. **2026-08-19 registration-link sweep:** the closing interest band still said "Team registration opens in September 2026" with a "Fill Out the Interest Form" button linking to the old interest-signup Google Form — updated to "Team registration ... is open now" with a "Register Your Team" button linking to the live formstack registration form (`create_team_application_2026_2027`, confirmed resolving live), now with `target="_blank" rel="noreferrer noopener"` as an external link. Not yet pasted live. |
| [2026-projects.html](2026-projects.html) | [/past-projects/2026-projects](https://beaver-works-assistive-tech.mit.edu/past-projects/2026-projects) | 2026-07-12 | ✅ Synced | 2026-07-12 | Removed stray `'DM Sans'` from body font-family, added missing `<meta name="description">`, then removed `target="_blank"` from 2 internal links (Past Projects index, Getting Started). Pasted live and confirmed by user 2026-07-12. |
| [schedule.html](schedule.html) | [/create-challenge/schedule](https://beaver-works-assistive-tech.mit.edu/create-challenge/schedule) | 2026-08-19 | 🔄 Needs sync | 2026-08-19 | Was ✅ Synced as of 2026-07-12 (style-guide drift fixes plus the duplicate-`<h1>` fix, pasted live and confirmed by user). **2026-08-19 update:** added a new Project-tagged row to the Spring Semester table — Due Date for Simplify, February 21 — between the Preliminary Design Review (Feb 7) and Maker Skills Section (Feb 28) rows. This is a local addition ahead of the live Google Site. `check-styles.py` passes with zero drift across all 27 pages. **Confirmed live 2026-08-19 (second check, after user pasted the update):** fetched the live page's rendered HTML directly — "February 21" and "Simplify" are both now present in the Spring Semester table. **Caveat found in the same check, resolved 2026-08-19:** this page has 5 "Due Date for..." links pointing at `.../create-challenge/create-project/design-reviews` — briefly a dead URL after that page moved in the Google Sites hierarchy, now fixed (see the `design-reviews.html` row) and confirmed resolving correctly again. No local change was needed. **2026-08-19 registration-link sweep (later same day):** two stale registration links found. (1) The Fall Semester "Due Date for Team Registration" link pointed at `create_teamapplication_2024_2025` — a two-seasons-old URL slug (also missing the underscore between "team" and "application" that the current form uses) — corrected to the live `create_team_application_2026_2027` form and given `target="_blank" rel="noreferrer noopener"` as an external link. (2) The closing interest band still said "Team registration opens in September 2026" with a "Fill Out the Interest Form" button linking to the old interest-signup Google Form — updated to "Team registration ... is open now" with a "Register Your Team" button linking to the same live formstack form. Both links confirmed resolving live. Not yet pasted live. |
| [faq.html](faq.html) | [/create-challenge/faq](https://beaver-works-assistive-tech.mit.edu/create-challenge/faq) | 2026-08-15 | ✅ Synced | 2026-08-15 | New HTML embed (first conversion from Google Sites — page was previously listed under "Not yet converted"). Built to resolve [issue #19](https://github.com/hoseasiu/create-challenge-website/issues/19): fixed heading hierarchy (every question is now a consistently-leveled `<h3>` under its section's `<h2>`; previously a mix of `<h2>`s and plain text, with one question mis-nested as a false section header), unified the team-number scheme everywhere it's mentioned (2 digits = middle school, 3 = high school, 4 = college — previously inconsistent between "three-digit" and "3- or 4-digit" and omitted middle school entirely), stated the middle school policy plainly (contact us to see what's possible; assigned 2-digit numbers if they join), and added a jump-links section index at the top. Checked live 2026-08-15: the jump-nav, the reworded middle-school FAQ item, and the unified 2/3/4-digit team-number explanation all match the local file exactly — already pasted live, this row's earlier "not yet pasted" note was stale. Re-confirmed live 2026-08-19 (fetched rendered HTML: middle-school and 2/4-digit team-number language both present). |
| [idea-to-ink.html](idea-to-ink.html) | [/create-challenge/create-course/idea-to-ink](https://beaver-works-assistive-tech.mit.edu/create-challenge/create-course/idea-to-ink) | 2026-08-15 | ✅ Synced | 2026-08-15 | New HTML embed (first conversion from Google Sites — Module 3 of the CRE[AT]E Course, previously listed under "Not yet converted"). Built following the `course-introduction.html` pattern (full 960px prose width, `.video-stack`/`.video-embed`, quiz banner, module-nav interest band). Content — storyboarding, sketching for physical products and software/UI, and raster vs. vector digital drawing — is verbatim from the live page as of 2026-08-15. Checked live 2026-08-15: matches the local file exactly, including both storyboard example images (`raw.githubusercontent.com` URLs) resolving correctly now that the commit is on `main` — already pasted live, this row's earlier "not yet pasted" note was stale. Re-confirmed live 2026-08-19 (fetched rendered HTML: "Storyboard" and "Raster"/"Vector" both present). |
| [documentation-and-presentation.html](documentation-and-presentation.html) | [/create-challenge/create-course/documentation-and-presentation](https://beaver-works-assistive-tech.mit.edu/create-challenge/create-course/documentation-and-presentation) | 2026-08-15 | ✅ Synced | 2026-08-15 | New HTML embed (first conversion from Google Sites — Module 5 of the CRE[AT]E Course, previously listed under "Not yet converted"). Content is verbatim from the live page as of 2026-08-15: the "why documentation matters" intro, the "Documentation and Storytelling" and "Guidelines for Clear Presentation" recorded-lecture videos (YouTube IDs `D-2sV5aNaV0` and `wX_g62y3c2A`), the 5-item design-review video tips list (including the OBS link), and the nested "Documentation and Presentation Review" assignment list. All internal links (Design Reviews, 2022-2023/2023-2024 Past Projects, module-nav buttons) use plain `href` with no `target`; the OBS link is external and uses `target="_blank"`. Note: the module-nav "Next" button points to `/create-challenge/create-project`, which is outside the CRE[AT]E Course and not yet a local HTML embed itself — this is expected, matching the live page's own "Next: CRE[AT]E Project" link. `check-styles.py` passes with zero drift across all 16 pages. **Confirmed live 2026-08-19:** fetched the live page's rendered HTML directly — the "Documentation and Storytelling" and "Guidelines for Clear Presentation" video sections, the assignment list, and both YouTube video IDs are all present. This row's earlier "Not yet pasted live" note was stale. **Caveat found 2026-08-19, resolved same day:** the "final submission requirements" link points to `.../create-challenge/create-project/design-reviews` — briefly a dead URL after that page moved in the Google Sites hierarchy, now fixed (see the `design-reviews.html` row) and confirmed resolving correctly again. No local change was needed. |
| [maker-skills-mechanical.html](maker-skills-mechanical.html) | [/create-challenge/create-course/maker-skills/mechanical](https://beaver-works-assistive-tech.mit.edu/create-challenge/create-course/maker-skills/mechanical) | 2026-08-15 | ✅ Synced | 2026-08-19 | New HTML embed (first conversion from Google Sites — one of the four Maker Skills sub-pages under Module 4, previously listed under "Not yet converted"; built by 4 parallel subagents alongside Electronics/Software/Wearables). Live URL slug confirmed as `mechanical` (the `Mechanical Making.md` snapshot's old `mechanical-making` slug was stale and has been corrected). Content — Machine Tools and Shops (with the StuffMadeHere shop tour video, YouTube ID `SjJulcvTA7Y`, and the MIT Beaver Works Matterport tour link), 3D Printing (with an intro video, YouTube ID `3LBTkLsjHGQ`, and the 5 verbatim comprehension questions), and Computer-Aided Design/Onshape — is verbatim from the live page as of 2026-08-15. `check-styles.py` passes with zero drift. **Known issue carried over from the live site, not introduced by this conversion:** the Matterport virtual tour link (`my.matterport.com/show/?m=rJtbANQgip3`) returns "Oops, model not available" — the 3D model appears to have been taken down; the live Google Sites page has the same dead link. **Confirmed live 2026-08-19:** fetched the live page's rendered HTML directly — Machine Tools and Shops, 3D Printing, and Computer-Aided Design/Onshape sections and both video IDs are all present. This row's earlier "Not yet pasted live" note was stale. |
| [maker-skills-electronics.html](maker-skills-electronics.html) | [/create-challenge/create-course/maker-skills/electronics](https://beaver-works-assistive-tech.mit.edu/create-challenge/create-course/maker-skills/electronics) | 2026-08-15 | ✅ Synced | 2026-08-19 | New HTML embed (first conversion from Google Sites — one of the four Maker Skills sub-pages under Module 4, previously listed under "Not yet converted"; built by 4 parallel subagents alongside Mechanical/Software/Wearables). Content — Electrical Engineering Principles (Khan Academy links) and Circuits and Microcontrollers (Arduino/Adafruit Feather/Raspberry Pi platforms, Tinkercad simulator workflow, resource list) — is verbatim from the live page as of 2026-08-15. No video embeds on this page (confirmed against the live page). `check-styles.py` passes with zero drift. All links checked live and working. **Confirmed live 2026-08-19:** fetched the live page's rendered HTML directly — Electrical Engineering Principles and Circuits and Microcontrollers sections, Arduino, and Tinkercad are all present. This row's earlier "Not yet pasted live" note was stale. |
| [maker-skills-software.html](maker-skills-software.html) | [/create-challenge/create-course/maker-skills/software](https://beaver-works-assistive-tech.mit.edu/create-challenge/create-course/maker-skills/software) | 2026-08-15 | ✅ Synced | 2026-08-19 | New HTML embed (first conversion from Google Sites — the largest of the four Maker Skills sub-pages under Module 4, previously listed under "Not yet converted"; built by 4 parallel subagents alongside Mechanical/Electronics/Wearables). Content covers all 8 major sections from the live page as of 2026-08-15: software project forms, Version Control (GitHub), UI/Front-End Development (Figma), Web Development (stub), Desktop Software Development (packaging), Mobile Application Development (iOS/Android/MIT App Inventor), Software Accessibility Considerations, and AI/ML Tools for Accessibility (including the Machine Learning Lifecycle video, YouTube ID `ZmBUnJ7lGvQ`, and the Data Bias/Training Models subsections). `check-styles.py` passes with zero drift (one stray unescaped `&` in "Security & isolation" was caught and fixed post-build). **Confirmed live 2026-08-19:** fetched the live page's rendered HTML directly — Version Control, User Interface and Front-End Development, Mobile Application Development, and AI and Machine Learning Tools for Accessibility sections, plus the Machine Learning Lifecycle video ID, are all present. |
| [maker-skills-wearables.html](maker-skills-wearables.html) | [/create-challenge/create-course/maker-skills/wearables](https://beaver-works-assistive-tech.mit.edu/create-challenge/create-course/maker-skills/wearables) | 2026-08-15 | ✅ Synced | 2026-08-19 | New HTML embed (first conversion from Google Sites — one of the four Maker Skills sub-pages under Module 4, previously listed under "Not yet converted"; built by 4 parallel subagents alongside Mechanical/Electronics/Software). Content — Electronic Wearables components, Key Considerations (Form Factor & Ergonomics, Measuring for Wearables, Aesthetics), and the AFFOA Wearable Technology Curriculum partnership — is verbatim from the live page as of 2026-08-15. No video embeds on this page (confirmed against the live page). `check-styles.py` passes with zero drift. All links checked live and working. **Confirmed live 2026-08-19:** fetched the live page's rendered HTML directly — Key Considerations, AFFOA, and Form Factor content are all present. This row's earlier "Not yet pasted live" note was stale. |
| [create-project.html](create-project.html) | [/create-challenge/create-project](https://beaver-works-assistive-tech.mit.edu/create-challenge/create-project) | 2026-08-19 | ✅ Synced | 2026-08-19 | New HTML embed (first conversion from Google Sites — CRE[AT]E Project section index, previously listed under "Not yet converted"). Content was verbatim from the live page as of 2026-08-15: intro paragraph plus five callout cards (Finding a Co-Designer, Co-Designer Interviews, Project Brainstorming, Scoping a Project, Design Reviews), matching the live page's card set exactly at that time, including that "Simplify" did not get its own card there. All links are internal, no `target` attribute. Built as part of a 6-page parallel conversion of the whole CRE[AT]E Project section; `simplify.html` was intentionally skipped at that time — see its own row. **2026-08-19 update:** a sixth callout card, **Simplify**, was inserted between Scoping a Project and Design Reviews now that `simplify.html` exists. `check-styles.py` passes with zero drift across all 27 pages. **Confirmed live 2026-08-19 (second check, after user pasted the update):** fetched the live page's rendered HTML directly — all six cards' links (including `create-project/simplify`) are present. **Caveat found in the same check, resolved 2026-08-19:** the Design Reviews card links to `.../create-challenge/create-project/design-reviews` — briefly a dead URL after that page moved in the Google Sites hierarchy, now fixed (see the `design-reviews.html` row) and confirmed resolving correctly again. No local change was needed. |
| [finding-a-co-designer.html](finding-a-co-designer.html) | [/create-challenge/create-project/finding-a-co-designer](https://beaver-works-assistive-tech.mit.edu/create-challenge/create-project/finding-a-co-designer) | 2026-08-15 | ✅ Synced | 2026-08-19 | New HTML embed (first conversion from Google Sites — first step of the CRE[AT]E Project section, previously listed under "Not yet converted"). Content ("Who can be a co-designer?" and "If you need to find a co-designer") is verbatim from the live page as of 2026-08-15, closing with a reflect-box "Assignment: Find a Co-Designer" that links forward to Co-Designer Interviews (internal, no `target`). No images on the live page. Part of the same 6-page parallel conversion noted on the `create-project.html` row above. `check-styles.py` passes with zero drift across all 26 pages. |
| [co-designer-interviews.html](co-designer-interviews.html) | [/create-challenge/create-project/co-designer-interviews](https://beaver-works-assistive-tech.mit.edu/create-challenge/create-project/co-designer-interviews) | 2026-08-15 | ✅ Synced | 2026-08-19 | New HTML embed (first conversion from Google Sites — second step of the CRE[AT]E Project section, previously listed under "Not yet converted"). Content — what a co-designer interview is, how to plan one, interviewing techniques and structure, what not to do, interview logistics, and interview protocols — is verbatim from the live page as of 2026-08-15, with two reflect-box assignments ("Interview Protocol" and "Needs and Requirements") and a closing module-nav interest band (Previous: Finding a Co-Designer / Next: Project Brainstorming, both internal links matching the live page's own Previous/Next text). No images on the live page. Part of the same 6-page parallel conversion noted on the `create-project.html` row above. `check-styles.py` passes with zero drift across all 26 pages. |
| [project-brainstorming.html](project-brainstorming.html) | [/create-challenge/create-project/project-brainstorming](https://beaver-works-assistive-tech.mit.edu/create-challenge/create-project/project-brainstorming) | 2026-08-15 | ✅ Synced | 2026-08-19 | New HTML embed (first conversion from Google Sites — third step of the CRE[AT]E Project section, previously listed under "Not yet converted"). Content — brainstorming basics, the Five Whys/SCAMPER/Add Constraints/Individual Brainstorming methods, and External Search — is verbatim from the live page as of 2026-08-15, with reflect-box callouts for the Warning and the two Assignments ("Brainstorming Document" and "External Search"), plus a closing module-nav interest band (Previous: Co-Designer Interviews / Next: Scoping a Project, matching the live page's own Previous/Next text). No images on the live page. Part of the same 6-page parallel conversion noted on the `create-project.html` row above. `check-styles.py` passes with zero drift across all 26 pages. |
| [scoping-a-project.html](scoping-a-project.html) | [/create-challenge/create-project/scoping-a-project](https://beaver-works-assistive-tech.mit.edu/create-challenge/create-project/scoping-a-project) | 2026-08-19 | ✅ Synced | 2026-08-19 | New HTML embed (first conversion from Google Sites — fourth step of the CRE[AT]E Project section, previously listed under "Not yet converted"). Content — the "Team exercise: Taking Stock" (Time/Funding/Expertise) reflect-box and "What we (generally) don't recommend" (including the safety-critical-products policy) — is verbatim from the live page as of 2026-08-15. No images on the live page. Part of the same 6-page parallel conversion noted on the `create-project.html` row above. **2026-08-19 update:** added a module-nav interest band (Previous: Project Brainstorming / Next: Simplify), now that `simplify.html` exists to link forward to. `check-styles.py` passes with zero drift across all 27 pages. **Confirmed live 2026-08-19 (second check, after user pasted the update):** fetched the live page's rendered HTML directly — links to both `project-brainstorming` and `create-project/simplify` are present in the nav band. |
| [design-reviews.html](design-reviews.html) | [/create-challenge/create-project/design-reviews](https://beaver-works-assistive-tech.mit.edu/create-challenge/create-project/design-reviews) | 2026-08-19 | 🔄 Needs sync | 2026-08-19 | New HTML embed (first conversion from Google Sites — fifth/final step of the CRE[AT]E Project section, previously listed under "Not yet converted"; the largest page in the repo). Content — Peer/Remote Mentor Reviews, Time Zones/Extensions, all five design-review Gates (White/Orange/Blue/Brown/Black) each built as a native `<details>`/`<summary>` accordion per the live page's own "expand each section" note, and a 10-item Design Review FAQ built with the same `.faq-item` component as `faq.html` — is verbatim from the live page as of 2026-08-15, with one heading renamed for clarity ("Design Reviews" → "Remote Mentor Reviews" for the second Peer/Mentor subsection, which duplicated the page title on the live site) and four stale/broken internal links corrected to their current URLs (Introduction, Design Processes, Documentation and Presentation, and the FAQ's Idea to Ink link — all previously pointed at pre-conversion "old-..." slugs or a dead self-referential anchor on the live site). No images on the live page. Part of the same 6-page parallel conversion noted on the `create-project.html` row above. **2026-08-19 update:** added a module-nav interest band with a single Previous: Simplify link (no Next — still the last step of the section), now that `simplify.html` exists. `check-styles.py` passes with zero drift across all 27 pages. **2026-08-19, first re-check after that update — found a URL/site-structure problem, not a simple sync gap:** the page had been moved up one level in the Google Sites page hierarchy (out from under CRE[AT]E Project), so `/create-challenge/create-project/design-reviews` hard-404'd live while the content actually lived at `/create-challenge/design-reviews`. This broke 4 internal links elsewhere in the repo that point at the nested path. **2026-08-19, second re-check — confirmed fixed:** the user moved the page back into its nested location in Google Sites. Fetching `/create-challenge/create-project/design-reviews` directly now returns the correct content (Gates, Peer review sections, FAQ, and the "Coming from Simplify?" nav band all present, no 404), and the flat `/create-challenge/design-reviews` URL now 404s as expected — the page lives in one place again, matching its siblings and every existing internal link. The 4 dependent links in `schedule.html`, `create-project.html`, `documentation-and-presentation.html`, and `simplify.html` needed no changes and now resolve correctly. **2026-08-19 registration-link sweep, follow-up:** the media release form link in the design-review FAQ (previously flagged in that sweep as "out of scope, not fixed" since I didn't know its current formstack slug) has been fixed a different way — per Hosea's instruction, it now points to the site's `/create-challenge/current-forms` page instead of a direct formstack URL, matching the identical media-release-form link two paragraphs earlier in the same FAQ (which already used that pattern). This avoids the direct-link staleness problem entirely: the old link was `mit-bwsi.formstack.com/forms/create_codesigner_media_release_24_25` (a year-old slug); the Current Forms page is confirmed live (title "Beaver Works AT - Current Forms") and is the same indirection `getting-started.html`-style pages should probably use for any form link likely to change yearly. Also dropped `target="_blank"` since it's now an internal link, per the corrected link-target rule in CLAUDE.md. Not yet pasted live. |
| [simplify.html](simplify.html) | [/create-challenge/create-project/simplify](https://beaver-works-assistive-tech.mit.edu/create-challenge/create-project/simplify) | 2026-08-19 | ✅ Synced | 2026-08-19 | New HTML embed (first conversion from Google Sites — the last unconverted page in the CRE[AT]E Project section, intentionally skipped in the 2026-08-15 batch conversion; see that batch's note below). Content — the "your project has failed" framing narrative and "Why simplify?" benefits list, the "Case Study: Chase's Watering Can" section (Team Vextraordinary, 2023–2024 season), and the "How can you simplify your design?" section with two assignments (Constrained Brainstorming, Red Teaming) — is verbatim from the live page as of 2026-08-19. **Embedded-document handling:** the live page's "Design Document: Chase's Watering Can" link is a Google Sites auto-generated rich link-preview card with a thumbnail image served from a signed, non-durable `lh3.googleusercontent.com` URL — not a stable asset this repo can depend on — so it was rebuilt as a standalone `.callout-card` (title, short body, arrow link) pointing to the same destination (`/create-challenge/2024-projects-section/2024-project-page-vextraordinary`) instead of reproducing the thumbnail. That destination page is itself not yet a local HTML embed (still native Google Sites) — link checked live and resolves correctly. Originally built with no Previous/Next module-nav interest band, matching the live page at that time (same as its siblings Scoping a Project and Design Reviews). **2026-08-19 update:** added a module-nav interest band (Previous: Scoping a Project / Next: Design Reviews); `create-project.html`, `scoping-a-project.html`, and `design-reviews.html` were updated in the same pass to cross-link to this page, completing the section's Previous/Next chain. `check-styles.py` passes with zero drift across all 27 pages. **Confirmed live 2026-08-19 (second check, after user created and pasted the page):** the page now exists live at the correct nested URL with correct title, and the "your project has failed" framing, the Chase's Watering Can case study, and the Previous/Next nav band are all present. **Caveat found in the same check, resolved 2026-08-19:** the "Next: Design Reviews →" button links to `.../create-challenge/create-project/design-reviews` — briefly a dead URL after that page moved in the Google Sites hierarchy, now fixed (see the `design-reviews.html` row) and confirmed resolving correctly again. No local change was needed. |
| [maker-skills-artificial-intelligence.html](maker-skills-artificial-intelligence.html) | [/create-challenge/create-course/maker-skills/artificial-intelligence](https://beaver-works-assistive-tech.mit.edu/create-challenge/create-course/maker-skills/artificial-intelligence) | 2026-08-19 | ✅ Synced | 2026-08-19 | New HTML embed (fifth and final Maker Skills sub-page under Module 4, intentionally skipped in the 2026-08-15 batch pending its content image; converted now that the user added `website-images/ai-diagram.png`). Content is verbatim from the live page as of 2026-08-19: What Is AI, AI Is Not Just Chatbots (with the nested AI/ML/Deep Learning/Generative AI/LLM/Chatbots diagram and a "But what is a neural network?" video, YouTube `aircAruvnKk`), Large Language Models (a "Large Language Models explained briefly" video, YouTube `LPZh9BOjkQs`) and What Can LLMs Do, The Ethics of Large Language Models (Intellectual Property, Moderation and Training), Educational Risks, Privacy and Consent (with Local AI Models), AI and Disability, and the two "Your Team's Approach to AI as a Tool/Component" policy sections. Two live-page headings ("Intellectual Property", "Moderation and Training") were bold-styled text rather than real `<h>` elements on the source page — promoted to genuine `<h3>` for accessibility, matching their existing visual styling exactly. The two "Assignment: Draft an AI ... Policy" sections, which are their own top-level headings on the live page, were folded into `.reflect-box` callouts nested in their parent section, matching the reflect-box-for-assignments convention used elsewhere in the CRE[AT]E Course/Project pages rather than literally preserving the live page's flatter heading structure. A closing "Content Disclosure" section (present on this page only, not site-wide) was kept as its own section. `check-styles.py` passes with zero drift across all 27 pages; all external links spot-checked live except one CNBC article link that 403s to automated fetches (matches the live page's own link — likely bot-blocking, not a dead link). **Confirmed live 2026-08-19:** the page (like all others) references the diagram via `raw.githubusercontent.com/.../main/...`, which only resolves once the branch merges to `main` — that merge has since happened (PR #42), and fetching the live page's rendered HTML directly confirmed: the page exists live with a real page title, all major sections (What is AI, AI is not just chatbots, Large Language Models, Educational Risks, AI and Disability, both "Your Team's Approach" policy sections) are present, and the `ai-diagram.png` image itself returns HTTP 200. This row's earlier "Not yet pasted live" note was stale — it's already been pasted live and just wasn't recorded here. |

## Local drafts not yet linked to a snapshot

These HTML files exist in the repo but their `site-snapshot` entry has no `source_html` field and is still marked `content_status: stub` — meaning it's unclear whether this embed was ever pushed to the live site at all. Confirm live status before relying on the sync table above for these.

| Local file | Live URL | Last local edit | Notes |
|---|---|---|---|
| [getting-started.html](getting-started.html) | [/create-challenge/getting-started](https://beaver-works-assistive-tech.mit.edu/create-challenge/getting-started) | 2026-08-19 | Style-guide drift fixes (`.section__list` spacing, `.feature-card__body` font-size), then removed `target="_blank"` from 1 internal link (Goals of the Challenge). Pasted live and confirmed by user 2026-07-12. `site-snapshot` entry still needs its `source_html` field added per the workflow in CLAUDE.md. **2026-08-19, registration-link simplification:** both registration links (the inline mention and the closing "Register Your Team" button) were switched from the direct formstack registration URL to `/create-challenge/current-forms`, per Hosea's request to route all registration links through that single hub page. Not yet pasted live. |
| [goals-of-the-challenge.html](goals-of-the-challenge.html) | [/create-challenge/goals-of-the-challenge](https://beaver-works-assistive-tech.mit.edu/create-challenge/goals-of-the-challenge) | 2026-08-19 | Wrapped the "most important learning objective" sentence in `<strong>` in the Learning Objectives section (2026-08-15). Checked live 2026-08-15: the `<strong>`-wrapped sentence matches the local file exactly — ✅ **already pasted live**, the earlier "not yet pasted" note was stale. `site-snapshot` entry still needs its `source_html` field added per the workflow in CLAUDE.md (unrelated to sync status). Re-confirmed live 2026-08-19 (fetched rendered HTML; the sentence is still wrapped in `<strong>`). **2026-08-19 registration-link sweep (🔄 needs sync as of this edit):** the closing interest band still said "Team registration opens in September 2026" with a "Fill Out the Interest Form" button linking to the old interest-signup Google Form, despite its own heading already saying "Register for the 2026–2027 season" — updated the body copy and button to match, now linking to the live formstack registration form (`create_team_application_2026_2027`, confirmed resolving live) with `target="_blank" rel="noreferrer noopener"`. Not yet pasted live. |

## Not yet converted to an HTML embed

These pages are still native Google Sites content with no local HTML embed. Not "out of sync" — just not started. Listed for backlog visibility only; move a page out of this list and into the table above once it gets a local HTML embed.

- Alumni Division, Registration Process, Current Forms
- Final Event: 2023, 2024, 2025, 2026
- Past Projects: 2023, 2024, 2025 (2026 is already an embed — see table above)
- Challenge Dashboard
- Previous Programs: Design for Education Aids (Crash Course, Projects), Design of AT (2021 Documentation, 2022 Assignments, 2022 Documentation, About Design of AT, Final Prototype, Initial Prototype, Product Requirements)

---

*Last reviewed: 2026-08-19 (see full-site sync audit note below). All 27 pages pass `check-styles.py` with zero drift against `reference-style.css`. This review checked every row previously marked 🔄 Needs sync (`maker-skills.html`, `faq.html`, `idea-to-ink.html`, `goals-of-the-challenge.html`) directly against the live Google Site by inspecting the rendered embed content (the Google Sites embed is a closed-shadow-DOM component, so content was extracted from the page's hydration HTML rather than a visual screenshot) — all four matched their local files exactly and their status was stale; they're now marked ✅ Synced. `design-processes.html` (merged via PR #36, "Add Design Processes page as local HTML embed") was found to have no row in this table at all despite already being live; it's now added, and two real bugs found live were fixed locally (internal module-nav links had `target="_blank"`, and `.reflect-box` had a 24px/32px margin-top drift) — this row is 🔄 Needs sync until those fixes are pasted live. `documentation-and-presentation.html` (Module 5 of the CRE[AT]E Course) was converted from Google Sites to a local HTML embed for the first time — see its row above; it's 🔄 Needs sync until pasted live.

**2026-08-15 Maker Skills conversion pass:** the four Maker Skills sub-pages (Mechanical, Electronics, Software, Wearables — Module 4 of the CRE[AT]E Course) were converted from Google Sites to local HTML embeds for the first time via 4 parallel subagents, each scoped to touch only its own new HTML file and its own `site-snapshot` entry to avoid write conflicts; see their rows above — all four are 🔄 Needs sync until pasted live. Content and URL slugs were confirmed directly against the live Google Site (the Mechanical Making snapshot's `mechanical-making` URL slug was found stale and corrected to `mechanical`), and every external link across the four pages was checked live: two pre-existing dead links were found and left as-is (matching the live site) rather than silently altered — the Matterport virtual tour on the Mechanical page ("model not available") and `randoma11y.com` on the Software page (invalid SSL certificate) — see their table rows for details. The Artificial Intelligence Maker Skills sub-page was intentionally left unconverted and added to "Not yet converted" below.

**2026-08-15 CRE[AT]E Project conversion pass:** the entire CRE[AT]E Project section (minus `Simplify`, which has a content image and was intentionally skipped) was converted from Google Sites to local HTML embeds in one parallel pass by six subagents, each independently reading the live page, building the HTML per the site style guide, updating its own `site-snapshot` entry, and running `check-styles.py` — `create-project.html` (section index), `finding-a-co-designer.html`, `co-designer-interviews.html`, `project-brainstorming.html`, `scoping-a-project.html`, and `design-reviews.html`. All six are new rows above, and all six pages' cross-links to each other were spot-checked afterward for correctness (index → all five sub-pages; each sub-page's Previous/Next module nav) and confirmed to point at the right internal URLs with no `target="_top"` anywhere in the repo. `design-reviews.html` additionally corrects four stale internal links on the live Google Site (see its row above) and introduces one new page-specific component (a `<details>`/`<summary>` accordion for the five design-review Gates) documented inline in its `<style>` block. This batch was confirmed pushed and ✅ Synced live as of 2026-08-19 (four of the six rows — `finding-a-co-designer.html`, `co-designer-interviews.html`, `project-brainstorming.html`, plus the four Maker Skills sub-pages from the same day's other batch — remain in that state; `create-project.html`, `scoping-a-project.html`, and `design-reviews.html` have since had further local-only edits, see below).

**2026-08-19 Simplify conversion:** `simplify.html`, the one page skipped in the 2026-08-15 CRE[AT]E Project batch, was converted — see its row above and its updated `site-snapshot` entry for details on how the live page's Google-Sites-native rich link-preview card (the "embedded document") was rebuilt as a `.callout-card` rather than reproducing its non-durable thumbnail image URL. This completes the CRE[AT]E Project section: all six pages now have local HTML embeds.

**2026-08-19 CRE[AT]E Project cross-linking:** with `simplify.html` now built, `create-project.html` (added a sixth callout card), `scoping-a-project.html`, and `design-reviews.html` (both gained their first-ever module-nav interest band) were updated to link it into the section's Previous/Next sequence — Scoping a Project → Simplify → Design Reviews — and `simplify.html` itself got matching Previous/Next links. All three rows flip back to 🔄 Needs sync as a result, even though their base content was independently confirmed live earlier the same day (see their rows above). `check-styles.py` passes with zero drift across all 27 pages.

**2026-08-19 Schedule update:** added a Simplify due-date row (February 21) to `schedule.html`'s Spring Semester table, between the Orange Gate / Preliminary Design Review (Feb 7) and Maker Skills Section (Feb 28) rows — see the `schedule.html` row above.

**2026-08-19 Maker Skills — Artificial Intelligence:** the fifth Maker Skills sub-page, intentionally skipped in the 2026-08-15 batch because its content image wasn't available yet, was converted now that the user added `website-images/ai-diagram.png`. See its row above.

**2026-08-19 full-site sync audit:** every row in this file was re-checked against the live site. Google Sites' embed content isn't exposed to normal DOM inspection or `WebFetch` (closed shadow DOM), so verification used a same-origin `fetch(window.location.href)` against each live page's own rendered HTML (via the Browser pane's `javascript_tool`) and searched it for specific section headings, link hrefs, video IDs, and other markers pulled from each local file — a more reliable technique than the "hydration HTML" screen-scraping used in earlier passes, and one worth reusing next time. Six Haiku subagents were run in parallel first to cover ground quickly; their results were uneven (a few gave real live-verified findings, several instead just reasoned from this file's own prior notes or compared against the stale `site-snapshot/` archive, which produced misleading "drift" findings against the wrong reference) — all findings below were independently re-confirmed directly against the live site before being recorded here. Findings:
- **Corrected several self-contradictory rows:** `design-processes.html`, `documentation-and-presentation.html`, and all four Maker Skills sub-pages (`maker-skills-mechanical.html`, `maker-skills-electronics.html`, `maker-skills-software.html`, `maker-skills-wearables.html`) were marked ✅ Synced in the status column but their own notes said "Not yet pasted live" — a stale-note bug, not a real sync gap. Live-fetched each one directly: all six are genuinely live and match their local files. Notes corrected; status column was already right.
- **`maker-skills-artificial-intelligence.html` flipped from 🔄 Needs sync to ✅ Synced:** it was recorded as blocked on the `ai-diagram.png` merge to `main`, but that merge (PR #42) has since happened, and a live fetch confirms the page, all its sections, and the diagram image (HTTP 200) are already live.
- **Confirmed still 🔄 Needs sync, with direct live evidence this time** (previously inferred from local edit history, not verified): `index.html`, `schedule.html`, `create-project.html`, `scoping-a-project.html`, `design-reviews.html`. `simplify.html` in particular 404s live — the page doesn't exist yet, so it can't be pasted into until it's created in the Google Sites site map first.
- **Spot-checked and confirmed accurate, no drift:** `about.html`, `maker-skills.html`, `faq.html`, `idea-to-ink.html`, `2026-projects.html` (team-number set matches exactly, 42 unique team numbers both sides), `goals-of-the-challenge.html`.
- **New non-sync finding:** `technical-mentors.html`'s footer reads "© 2025 MIT Beaver Works" on both the local file and the live page (confirmed identical) — a real content bug, but not a sync gap since both copies agree. Worth a follow-up fix.
- `finding-a-co-designer.html`, `co-designer-interviews.html`, `project-brainstorming.html`, `additional-resources.html`, `create-course.html`, `course-introduction.html`, `tips-for-teams.html`, and `getting-started.html` were **not** independently re-verified this pass (no local edits since their last confirmed sync, and no contradiction flags) — treat their ✅ Synced status as carried forward rather than freshly confirmed.

**2026-08-19 second-pass re-check (same day, after the user pasted updates live):** re-verified the 6 pages still marked 🔄 Needs sync from the pass above, plus the `technical-mentors.html` footer-year finding, using the same live-fetch technique.
- **Confirmed resolved, flipped to ✅ Synced:** `index.html`, `schedule.html`, `create-project.html`, `scoping-a-project.html`, `simplify.html` — all five now have their pending local changes live and verified (testimonials/registration messaging, the Simplify schedule row, the sixth callout card, the nav bands, and the Simplify page itself all present and correct).
- **`technical-mentors.html` footer-year bug: confirmed fixed** — local file and live page both now read "© 2026" (commit `e30cb84`).
- **`design-reviews.html`: initially NOT resolved, and a different problem than "needs sync."** The old URL (`/create-challenge/create-project/design-reviews`) hard-404'd live, reproduced on two fresh page loads. The site's own nav link for "Design Reviews" pointed to `/create-challenge/design-reviews` — the page had been moved up one level in the Google Sites page tree during editing, out from under CRE[AT]E Project, unlike its five sibling pages which stayed correctly nested. The content itself, fetched at that URL, was fully correct and up to date (including the new nav band) — so it wasn't a content/sync issue, it was a site-structure issue. It also broke 4 internal links elsewhere in the repo that pointed at the old nested path: 5 links in `schedule.html`, 1 in `create-project.html`, 1 in `documentation-and-presentation.html`, and 1 in `simplify.html`. Flagged to Hosea rather than guessed at, since there were two valid fixes (move the page back, or flatten every link to match). **Resolved 2026-08-19, third re-check:** Hosea moved the page back into its nested location in Google Sites. Re-verified directly: `/create-challenge/create-project/design-reviews` now returns the correct content with no 404, the flat `/create-challenge/design-reviews` URL now 404s as expected, and all 4 dependent links elsewhere in the repo resolve correctly again with no local changes needed. `design-reviews.html` flipped to ✅ Synced — this closes out the last open row from this audit; all 27 pages are now ✅ Synced.

**2026-08-19 registration-link sweep:** checked every registration/notify link site-wide against the live formstack registration form (`https://mit-bwsi.formstack.com/forms/create_team_application_2026_2027`, confirmed resolving live) already in use on `index.html` and `getting-started.html`. Found and fixed two categories of stale link, all on pages that predated registration opening and still carried "registration opens in September" / "get notified" / "interest form" language from before the season started:
- **Old interest-signup Google Form** (`docs.google.com/forms/d/e/1FAIpQLSfjykFOIogTkRrczyDZ8J1PyDbEiwDTHsGh3cNt8zFXnhzZhw/...`) — replaced with the live registration form, and surrounding copy updated from "opens in September" / "get notified" to "is open now" / "register", on `about.html` (2 spots), `goals-of-the-challenge.html`, `tips-for-teams.html`, and `schedule.html`'s closing interest band.
- **Stale/malformed formstack slug** on `schedule.html`'s Fall Semester table — `create_teamapplication_2024_2025` (two seasons old, and missing the underscore between "team" and "application" that the current slug uses) — corrected to `create_team_application_2026_2027`.
- All updated links now use `target="_blank" rel="noreferrer noopener"` per the external-link convention, and were spot-checked live (see individual rows above). None of these five pages have been pasted live yet with these fixes — flip to ✅ Synced only after pasting and confirming.
- **Out of scope, flagged not fixed:** separately, the live formstack registration form's own page header text still reads "Application is due October 5th, 2025 / kickoff Saturday October 18th, 2025" — stale copy on Formstack's side, outside this repo, worth flagging to whoever manages that form.

**2026-08-19 media release form fix (follow-up to the sweep above):** `design-reviews.html`'s media release form link (`create_codesigner_media_release_24_25`, a year-old slug — flagged above as "out of scope, not fixed" since the current slug wasn't known) was instead pointed at `/create-challenge/current-forms`, matching an identical media-release-form link two paragraphs earlier in the same FAQ that already used that pattern. Confirmed the Current Forms page resolves live (title "Beaver Works AT - Current Forms").

**2026-08-19 registration-link simplification (supersedes the direct-formstack-link fixes above):** per Hosea's request to simplify further, every registration link site-wide — including the ones fixed earlier the same day to point directly at `mit-bwsi.formstack.com/forms/create_team_application_2026_2027` — was switched to point at `/create-challenge/current-forms` instead, using that page as a single outbound-redirect hub rather than hardcoding the formstack slug in multiple places. This means the site never needs a repo-wide link sweep again when the registration form's URL changes year to year — only the Current Forms page itself needs updating. Affected: `index.html` (status line + interest band), `about.html` (2 spots), `getting-started.html` (2 spots), `goals-of-the-challenge.html`, `tips-for-teams.html`, and `schedule.html` (Fall Semester table row + interest band) — 10 links across 6 pages, plus the 2 already pointing at Current Forms in `design-reviews.html`. All dropped `target="_blank" rel="noreferrer noopener"` since the destination is now internal, per CLAUDE.md's link-target rule. `check-styles.py` passes with zero drift across all 28 pages. **Trade-off worth flagging:** the Current Forms page lists multiple forms (registration, design review submission, final report, co-designer consent) rather than jumping straight to the registration form, so these CTAs — including the homepage's primary "Register your team" buttons — now take visitors to a list instead of the form directly. Not yet pasted live on any of the 6 pages.

Update this file whenever a local HTML embed changes or whenever a push to the live site is confirmed — see the workflow in [CLAUDE.md](CLAUDE.md).*
