# Website Update Status

This file tracks which full-page HTML embeds in this repo are **in sync with the live Google Site**. It is the working handoff point between two tools:

- **Claude Code** edits the HTML files in this repo.
- **Claude in Chrome** (or the user, manually) pastes the updated HTML into the matching embed block on the live Google Site.

See the "Local-to-Live Sync Workflow" section of [CLAUDE.md](CLAUDE.md) for the full process. `site-snapshot/` remains the separate content archive of record — this file is purely about push status.

## How to read the table

| Column | Meaning |
|---|---|
| Local file | HTML file in the repo root |
| Live URL | The embed's page on beaver-works-assistive-tech.mit.edu |
| Last local edit | Date of the most recent commit touching the local file |
| Sync status | ✅ Synced · 🔄 Needs sync · ❓ Unknown |
| Last synced | Date someone confirmed the live embed matches the local file |

**Sync status meanings:**
- ✅ **Synced** — the live embed matches this local file as of "Last synced."
- 🔄 **Needs sync** — local file has changed since it was last pushed; live site is stale.
- ❓ **Unknown** — sync status has never been confirmed either way; treat as needs-sync until checked.

---

## Pages with a local HTML embed

The rows below were seeded on 2026-07-12 by comparing each file's last local-edit date against its `site-snapshot` entry's `snapshot_date`. Per the existing [Google Sites to HTML Embed Workflow](CLAUDE.md#google-sites-to-html-embed-workflow), `snapshot_date` should be bumped every time an embed is pushed live — so a local edit dated *after* `snapshot_date` means the live site is very likely stale. This is an inference, not a confirmed check; do one pass to verify each row, then keep it current going forward.

| Local file | Live URL | Last local edit | Sync status | Last synced | Notes |
|---|---|---|---|---|---|
| [index.html](index.html) | [/home](https://beaver-works-assistive-tech.mit.edu/home) | 2026-08-21 | ✅ Synced | 2026-08-21 | **2026-08-21, confirmed pasted live (issue #25 reorg sync check):** Live-fetched the page's rendered HTML directly (same-origin `fetch` technique) — the `Students and Coaches` link text and destination are live, and the internal link no longer carries `target="_blank"`. **2026-08-21 (issue #25 reorg link/copy sweep):** Button text/target updated ("Students and Coaches Guide", dropped `target="_blank"` on the now-internal link). **Note:** this row's "✅ Synced 2026-07-12" status was stale — a 2026-08-05 edit (commit `46c4b65`, issue #10: swapped stale "2026 Challenge has concluded" messaging for a 2026-2027 registration-open announcement) was never reflected here or confirmed pushed live. **2026-08-19 (issue #11):** added a compact season-status line above the hero, and a new "What teams and co-designers say" section (3 testimonial quotes + Levi/Izzy/Skylar co-designer story videos, pulled up from About) placed right after the hero for above-the-fold social proof. `check-styles.py` passes with zero drift. **Confirmed live 2026-08-19 (second check, after user pasted the update):** fetched the live page's rendered HTML directly — both the registration-open messaging and the "What teams and co-designers say" testimonials section are now present. **2026-08-19, registration-link simplification (later same day):** both registration CTAs (status line, interest band) were switched from the direct formstack registration URL to `/create-challenge/current-forms`, per Hosea's request to route all registration links through that single hub page instead of hardcoding the formstack slug in multiple places. **Confirmed live 2026-08-19 (third check):** fetched the live page's rendered HTML directly — no direct formstack link or old interest-form link remain, and `current-forms` links are present. |
| [about.html](about.html) | [/create-challenge/about](https://beaver-works-assistive-tech.mit.edu/create-challenge/about) | 2026-08-21 | 🔄 Needs sync | 2026-08-21 | **2026-08-21, nav-flow fix:** restored the About → Goals of the Challenge → Students and Coaches reading order (previously About skipped Goals entirely). Added a Goals of the Challenge mention/link to the intro paragraph, a matching button in the "Get Started" CTA row, and a new `Next: Goals of the Challenge →` page-nav strip at the bottom. `check-styles.py` passes with zero drift. **2026-08-21, confirmed pasted live (issue #25 reorg sync check):** Live-fetched the page's rendered HTML directly (same-origin `fetch` technique) — `Students and Coaches` link text/destination are live and the old `/create-challenge/getting-started` path is gone. **2026-08-21 (issue #25 reorg link/copy sweep):** 2 "Getting Started" link labels renamed to "Students and Coaches" (href unaffected — page already pointed at the new URL). Added missing `body { font-size: 1rem }` (style-guide drift fix), then removed `target="_blank"` from 1 internal link (Getting Started) per the corrected link-target rule in CLAUDE.md. Rebuilt 2026-07-12 from the last git commit after an earlier truncated paste; re-pasted live and confirmed by user 2026-07-12. Re-confirmed live 2026-08-19 (fetched rendered HTML, "130 student teams" present). **2026-08-19 registration-link sweep:** two stale "notify me"-era references to the old interest-signup Google Form (`docs.google.com/forms/d/e/1FAIpQLSfjykFOIogTkRrczyDZ8J1PyDbEiwDTHsGh3cNt8zFXnhzZhw/...`) were replaced with the live 2026–2027 registration form (`mit-bwsi.formstack.com/forms/create_team_application_2026_2027`, confirmed resolving live): the "Registration opens in September!" paragraph now reads "2026–2027 registration is open!" with a "register your team" link, and the "Get notified about the start of the 2026–2027 challenge" button now reads "Register for the 2026–2027 challenge". **Superseded same day by the registration-link simplification pass** (see `index.html`'s row and the note near the bottom of this file) — both links now point at `/create-challenge/current-forms` instead of the direct formstack URL. **Confirmed live 2026-08-19:** fetched the live page's rendered HTML directly — "registration is open", "register your team", and "Register for the 2026" are all present, with no direct formstack link remaining. |
| [schedule.html](schedule.html) | [/create-challenge/schedule](https://beaver-works-assistive-tech.mit.edu/create-challenge/schedule) | 2026-08-21 | ✅ Synced | 2026-08-21 | **2026-08-21, confirmed pasted live (issue #25 reorg sync check):** Live-fetched the page's rendered HTML directly (same-origin `fetch` technique) — the updated `/learn/create-course`, `/current-teams/design-reviews`, and `/current-teams/tips-for-teams` links are live, and the old nested `/create-challenge/create-project/design-reviews` path is gone. **2026-08-21 (issue #25 reorg link/copy sweep):** All CRE[AT]E Course/Project/Design Reviews/Tips for Teams/Maker Skills links updated to their new `/learn/*` and `/current-teams/*` URLs (12 links across the Fall/Spring tables and the closing module-nav). Was ✅ Synced as of 2026-07-12 (style-guide drift fixes plus the duplicate-`<h1>` fix, pasted live and confirmed by user). **2026-08-19 update:** added a new Project-tagged row to the Spring Semester table — Due Date for Simplify, February 21 — between the Preliminary Design Review (Feb 7) and Maker Skills Section (Feb 28) rows. This is a local addition ahead of the live Google Site. `check-styles.py` passes with zero drift across all 27 pages. **Confirmed live 2026-08-19 (second check, after user pasted the update):** fetched the live page's rendered HTML directly — "February 21" and "Simplify" are both now present in the Spring Semester table. **Caveat found in the same check, resolved 2026-08-19:** this page has 5 "Due Date for..." links pointing at `.../create-challenge/create-project/design-reviews` — briefly a dead URL after that page moved in the Google Sites hierarchy, now fixed (see the `design-reviews.html` row) and confirmed resolving correctly again. No local change was needed. **2026-08-19 registration-link sweep (later same day):** two stale registration links found. (1) The Fall Semester "Due Date for Team Registration" link pointed at `create_teamapplication_2024_2025` — a two-seasons-old URL slug (also missing the underscore between "team" and "application" that the current form uses) — corrected to the live `create_team_application_2026_2027` form and given `target="_blank" rel="noreferrer noopener"` as an external link. (2) The closing interest band still said "Team registration opens in September 2026" with a "Fill Out the Interest Form" button linking to the old interest-signup Google Form — updated to "Team registration ... is open now" with a "Register Your Team" button linking to the same live formstack form. Both links confirmed resolving live. **Superseded same day by the registration-link simplification pass** — both the Fall Semester table link and the interest-band button now point at `/create-challenge/current-forms` (internal, no `target`) instead of the direct formstack URL. **Confirmed live 2026-08-19:** fetched the live page's rendered HTML directly — no stale `create_teamapplication_2024_2025` slug remains, "is open now" / "Register Your Team" are present, and `current-forms` links resolve. |
| [faq.html](faq.html) | [/create-challenge/faq](https://beaver-works-assistive-tech.mit.edu/create-challenge/faq) | 2026-08-21 | ✅ Synced | 2026-08-21 | **2026-08-21, confirmed pasted live (issue #25 reorg sync check):** Live-fetched the page's rendered HTML directly (same-origin `fetch` technique) — the updated `/learn/create-project/finding-a-co-designer` link is live. **2026-08-21 (issue #25 reorg link/copy sweep):** Finding a Co-Designer link updated to `/learn/create-project/finding-a-co-designer`. New HTML embed (first conversion from Google Sites — page was previously listed under "Not yet converted"). Built to resolve [issue #19](https://github.com/hoseasiu/create-challenge-website/issues/19): fixed heading hierarchy (every question is now a consistently-leveled `<h3>` under its section's `<h2>`; previously a mix of `<h2>`s and plain text, with one question mis-nested as a false section header), unified the team-number scheme everywhere it's mentioned (2 digits = middle school, 3 = high school, 4 = college — previously inconsistent between "three-digit" and "3- or 4-digit" and omitted middle school entirely), stated the middle school policy plainly (contact us to see what's possible; assigned 2-digit numbers if they join), and added a jump-links section index at the top. Checked live 2026-08-15: the jump-nav, the reworded middle-school FAQ item, and the unified 2/3/4-digit team-number explanation all match the local file exactly — already pasted live, this row's earlier "not yet pasted" note was stale. Re-confirmed live 2026-08-19 (fetched rendered HTML: middle-school and 2/4-digit team-number language both present). **2026-08-19, site-wide "here"-link sweep:** the Edly help-guide sentence used the literal link text "here" — non-descriptive per CLAUDE.md's accessibility rules and explicitly in-scope for a fix-and-log correction. Moved the link onto "help guide" (already stated in plain text right before it) and dropped the trailing "here"; wording otherwise unchanged. **Resolved 2026-08-21** — confirmed re-pasted live at the new `/create-challenge/faq` URL with the "help guide" link-text fix (see the confirmation note at the start of this row). |
| [technical-mentors.html](technical-mentors.html) | [/take-part/technical-mentors](https://beaver-works-assistive-tech.mit.edu/take-part/technical-mentors) | 2026-08-21 | ✅ Synced | 2026-08-21 | **2026-08-21, confirmed pasted live (issue #25 reorg sync check):** Live-fetched the page's rendered HTML directly (same-origin `fetch` technique) — the new `/take-part/technical-mentors` URL is live and resolving correctly. **2026-08-21 (issue #25 reorg link/copy sweep):** Live URL moved to `/take-part/technical-mentors` — no content edit needed (its only internal link, to About, was unaffected), but this changes which embed block it should be pasted into next. Style-guide drift fixes (body font-size, `.section__body`/`.section__list` spacing, `.interest-band__body` opacity, `.section__list` color, `.schedule-block` margin). Pasted live and confirmed by user 2026-07-12. No internal links needed the target fix — all its links are already external. **Note (found 2026-08-19, resolved same day):** this page's footer read "© 2025 MIT Beaver Works" — a year behind the "© 2026" text used on every other page — present in both the local file and the live page. **Fixed and re-confirmed 2026-08-19** (commit `e30cb84`, "fix year typo"): local file now reads "© 2026 MIT Beaver Works", and fetching the live page's rendered HTML confirms "2026 MIT Beaver Works" is live and "2025 MIT Beaver Works" is gone. Note the wording is still just "MIT Beaver Works" rather than the "MIT Beaver Works Assistive Technology" used elsewhere — a minor style inconsistency, not re-flagged as blocking. |
| [students-and-coaches.html](students-and-coaches.html) | [/take-part/students-and-coaches](https://beaver-works-assistive-tech.mit.edu/take-part/students-and-coaches) | 2026-08-21 | 🔄 Needs sync | 2026-08-21 | **2026-08-21, nav-flow fix:** added the site's `.page-nav` component (this page didn't have one) reading `« Back: Goals of the Challenge` / `Next: Tips for Teams »`, correcting the chain order (Goals now precedes this page, not follows it). Also fixed a real bug found in the process: the old bottom-band "Next section: Goals of the Challenge" button carried `target="_blank"` on an internal link, against the CLAUDE.md link-target rule — replaced with the standard "About the Challenge" CTA used on the other chain pages (Goals/Tips for Teams/Schedule), with no `target` attribute. `check-styles.py` passes with zero drift. **2026-08-21, confirmed pasted live (issue #25 reorg sync check):** Live-fetched the page's rendered HTML directly (same-origin `fetch` technique) — the new `/take-part/students-and-coaches` URL (renamed from `/create-challenge/getting-started`) is live and resolving correctly. **2026-08-21 (issue #25 reorg link/copy sweep):** renamed from `getting-started.html` — the live page moved from `/create-challenge/getting-started` (nav label "Getting Started") to `/take-part/students-and-coaches` (nav label "Students and Coaches") as part of the site-wide audience-based navigation reorganization. `<title>` and the `sr-only` `<h1>` updated to match; page content is otherwise unchanged (a separate follow-up issue covers whether the content itself should be rewritten for the Students-and-Coaches audience framing). `site-snapshot` entry renamed to `Students and Coaches.md` with its `source_html` field updated to `students-and-coaches.html` — this row moves up from "Local drafts not yet linked to a snapshot" as a result. **Moving this file forward, every other page's inbound link/button text that said "Getting Started" (about.html ×2, additional-resources.html ×2, goals-of-the-challenge.html, index.html) was renamed to "Students and Coaches" to match.** Previous history (as `getting-started.html`): style-guide drift fixes (`.section__list` spacing, `.feature-card__body` font-size), then removed `target="_blank"` from 1 internal link (Goals of the Challenge). Pasted live and confirmed by user 2026-07-12. **2026-08-19, registration-link simplification:** both registration links (the inline mention and the closing "Register Your Team" button) were switched from the direct formstack registration URL to `/create-challenge/current-forms`, per Hosea's request to route all registration links through that single hub page. **Confirmed live 2026-08-19:** fetched the live page's rendered HTML directly — `current-forms` links are present, no direct formstack link remains. Pasted live under its old URL, and **now confirmed re-pasted into the new `/take-part/students-and-coaches` embed block as of 2026-08-21** (see the confirmation note at the start of this row). |
| [create-course.html](create-course.html) | [/learn/create-course](https://beaver-works-assistive-tech.mit.edu/learn/create-course) | 2026-08-21 | ✅ Synced | 2026-08-21 | **2026-08-21, confirmed pasted live (issue #25 reorg sync check):** Live-fetched the page's rendered HTML directly (same-origin `fetch` technique) — the new `/learn/create-course` URL is live, the Artificial Intelligence card, corrected `maker-skills/mechanical` slug, and "five areas" copy are all present, and the stale `mechanical-making` slug is gone. **2026-08-21 (issue #25 reorg link/copy sweep):** Live URL moved to `/learn/create-course`; all module-nav/card links updated to the new `/learn/create-course/*` and `/learn/create-project` prefixes; added the missing Artificial Intelligence card (was AI-less at 4 cards) and fixed the long-stale `maker-skills/mechanical-making` slug to `maker-skills/mechanical`; "four areas" copy corrected to "five areas". Fixed link underline/hover-opacity to match style guide, then removed `target="_blank"` from 11 internal links (module nav, maker-skills sub-links). Pasted live and confirmed by user 2026-07-12. |
| [course-introduction.html](course-introduction.html) | [/learn/create-course/introduction](https://beaver-works-assistive-tech.mit.edu/learn/create-course/introduction) | 2026-08-21 | ✅ Synced | 2026-08-21 | **2026-08-21, confirmed pasted live (issue #25 reorg sync check):** Live-fetched the page's rendered HTML directly (same-origin `fetch` technique) — the new `/learn/create-course/introduction` URL is live with `/learn/create-course` module-nav links present. **2026-08-21 (issue #25 reorg link/copy sweep):** Live URL moved to `/learn/create-course/introduction`; module-nav links updated to `/learn/create-course/*`. Aligned unused `.video-grid` CSS and `.video-embed` border-radius to reference values (no layout change — page uses `.video-stack`), then removed `target="_blank"` from 2 internal links. Pasted live and confirmed by user 2026-07-12. |
| [design-processes.html](design-processes.html) | [/learn/create-course/design-processes](https://beaver-works-assistive-tech.mit.edu/learn/create-course/design-processes) | 2026-08-21 | ✅ Synced | 2026-08-21 | **2026-08-21, confirmed pasted live (issue #25 reorg sync check):** Live-fetched the page's rendered HTML directly (same-origin `fetch` technique) — the new `/learn/create-course/design-processes` URL is live with `/learn/create-course` module-nav links present. **2026-08-21 (issue #25 reorg link/copy sweep):** Live URL moved to `/learn/create-course/design-processes`; module-nav links updated to `/learn/create-course/*`. New HTML embed (first conversion from Google Sites, merged via PR #36 — Module 2 of the CRE[AT]E Course, previously listed under "Not yet converted"; this row was missing from this table entirely until now). Checked live 2026-08-15: content (Co-Designers, The Design Process, Product Development Process Flows, What Is Prototyping, etc.) matches the local file, **but** the live page has two style-guide violations that were also present in the local file at push time: (1) the two module-nav buttons in the closing interest band had `target="_blank" rel="noreferrer noopener"` on internal links, violating the corrected internal-link rule in CLAUDE.md; (2) `.reflect-box` `margin-top` was 24px instead of the reference 32px (`check-styles.py` drift). Both fixed locally just now — `check-styles.py` passes with zero drift across all 15 pages. **Confirmed live 2026-08-19:** fetched the live page's rendered HTML directly — the module-nav links no longer have `target="_blank"`, and `.reflect-box` margin-top is 32px. Both fixes are live; this row's earlier "Not yet pasted live with these fixes" note was stale. **2026-08-19, verbatim audit against `old-design-processes`:** found and removed a fabricated module-overview paragraph not present in the source, restored 2 reworded sentences and 1 wording swap to verbatim, and removed 4 invented image figcaptions the source doesn't have — see [design-processes-idea-to-ink-verbatim-audit.md](design-processes-idea-to-ink-verbatim-audit.md) for the full list including several previously-unlogged-but-in-scope typo/grammar fixes. `check-styles.py` passes with zero drift across all 28 pages. **Resolved 2026-08-21** — confirmed pasted live at the new `/learn/create-course/design-processes` URL with the verbatim-audit fixes (see the confirmation note at the start of this row). |
| [idea-to-ink.html](idea-to-ink.html) | [/learn/create-course/idea-to-ink](https://beaver-works-assistive-tech.mit.edu/learn/create-course/idea-to-ink) | 2026-08-21 | ✅ Synced | 2026-08-21 | **2026-08-21, confirmed pasted live (issue #25 reorg sync check):** Live-fetched the page's rendered HTML directly (same-origin `fetch` technique) — the new `/learn/create-course/idea-to-ink` URL is live with `/learn/create-course` module-nav links present. **2026-08-21 (issue #25 reorg link/copy sweep):** Live URL moved to `/learn/create-course/idea-to-ink`; module-nav links updated to `/learn/create-course/*`. New HTML embed (first conversion from Google Sites — Module 3 of the CRE[AT]E Course, previously listed under "Not yet converted"). Built following the `course-introduction.html` pattern (full 960px prose width, `.video-stack`/`.video-embed`, quiz banner, module-nav interest band). Content — storyboarding, sketching for physical products and software/UI, and raster vs. vector digital drawing — is verbatim from the live page as of 2026-08-15. Checked live 2026-08-15: matches the local file exactly, including both storyboard example images (`raw.githubusercontent.com` URLs) resolving correctly now that the commit is on `main` — already pasted live, this row's earlier "not yet pasted" note was stale. Re-confirmed live 2026-08-19 (fetched rendered HTML: "Storyboard" and "Raster"/"Vector" both present). **2026-08-19, verbatim audit against `old-idea-to-ink`:** the storyboard resource-list descriptions had been condensed (dropped real content, e.g. "very articulately," "in the context of designing user experiences") and the 2 storyboard image figcaptions had invented detail beyond the source's terse "Credit to X" captions — both restored to verbatim; see [design-processes-idea-to-ink-verbatim-audit.md](design-processes-idea-to-ink-verbatim-audit.md). `check-styles.py` passes with zero drift across all 28 pages. **Resolved 2026-08-21** — confirmed pasted live at the new `/learn/create-course/idea-to-ink` URL with the verbatim-audit fixes (see the confirmation note at the start of this row). |
| [documentation-and-presentation.html](documentation-and-presentation.html) | [/learn/create-course/documentation-and-presentation](https://beaver-works-assistive-tech.mit.edu/learn/create-course/documentation-and-presentation) | 2026-08-21 | ✅ Synced | 2026-08-21 | **2026-08-21, confirmed pasted live (issue #25 reorg sync check):** Live-fetched the page's rendered HTML directly (same-origin `fetch` technique) — the updated `/current-teams/design-reviews` and `/learn/create-project` links are live, and the old `/create-challenge/create-project` prefix is gone. **2026-08-21 (issue #25 reorg link/copy sweep):** Live URL moved to `/learn/create-course/documentation-and-presentation`; Design Reviews link updated to `/current-teams/design-reviews`, past-projects links to `/showcase/*`, and the forward CRE[AT]E Project link to `/learn/create-project`. New HTML embed (first conversion from Google Sites — Module 5 of the CRE[AT]E Course, previously listed under "Not yet converted"). Content is verbatim from the live page as of 2026-08-15: the "why documentation matters" intro, the "Documentation and Storytelling" and "Guidelines for Clear Presentation" recorded-lecture videos (YouTube IDs `D-2sV5aNaV0` and `wX_g62y3c2A`), the 5-item design-review video tips list (including the OBS link), and the nested "Documentation and Presentation Review" assignment list. All internal links (Design Reviews, 2022-2023/2023-2024 Past Projects, module-nav buttons) use plain `href` with no `target`; the OBS link is external and uses `target="_blank"`. Note: the module-nav "Next" button points to `/create-challenge/create-project`, which is outside the CRE[AT]E Course and not yet a local HTML embed itself — this is expected, matching the live page's own "Next: CRE[AT]E Project" link. `check-styles.py` passes with zero drift across all 16 pages. **Confirmed live 2026-08-19:** fetched the live page's rendered HTML directly — the "Documentation and Storytelling" and "Guidelines for Clear Presentation" video sections, the assignment list, and both YouTube video IDs are all present. This row's earlier "Not yet pasted live" note was stale. **Caveat found 2026-08-19, resolved same day:** the "final submission requirements" link points to `.../create-challenge/create-project/design-reviews` — briefly a dead URL after that page moved in the Google Sites hierarchy, now fixed (see the `design-reviews.html` row) and confirmed resolving correctly again. No local change was needed. |
| [maker-skills.html](maker-skills.html) | [/learn/create-course/maker-skills](https://beaver-works-assistive-tech.mit.edu/learn/create-course/maker-skills) | 2026-08-21 | ✅ Synced | 2026-08-21 | **2026-08-21, confirmed pasted live (issue #25 reorg sync check):** Live-fetched the page's rendered HTML directly (same-origin `fetch` technique) — the new `/learn/create-course/maker-skills` URL is live with the Artificial Intelligence sub-page link present. **2026-08-21 (issue #25 reorg link/copy sweep):** Live URL moved to `/learn/create-course/maker-skills`; sub-page and module-nav links updated to `/learn/create-course/*`. Added a new "Artificial Intelligence" section card (icon, description, link to `/create-challenge/create-course/maker-skills/artificial-intelligence`) after the Wearables card. Checked live 2026-08-15: the AI card (title, body copy, icon path, link) is present and matches the local file exactly — already pasted live, this row's earlier "not yet pasted" note was stale. Re-confirmed live 2026-08-19 (link to the Artificial Intelligence sub-page still present in the rendered HTML). |
| [maker-skills-mechanical.html](maker-skills-mechanical.html) | [/learn/create-course/maker-skills/mechanical](https://beaver-works-assistive-tech.mit.edu/learn/create-course/maker-skills/mechanical) | 2026-08-21 | ✅ Synced | 2026-08-21 | **2026-08-21, confirmed pasted live (issue #25 reorg sync check):** Live-fetched the page's rendered HTML directly (same-origin `fetch` technique) — the new `/learn/create-course/maker-skills/mechanical` URL is live with `/learn/create-course` module-nav links present. **2026-08-21 (issue #25 reorg link/copy sweep):** Live URL moved to `/learn/create-course/maker-skills/mechanical`; module-nav links updated to `/learn/create-course/*`. **2026-08-19 correction:** the 2026-08-15 conversion note below claiming "verbatim from the live page" was wrong — Hosea caught significant rewording/condensing when comparing against the still-live hidden `old-mechanical` page, including one fabricated claim (Onshape "built for collaboration...", not in the source). Full text has been reverted to verbatim source wording; see [maker-skills-verbatim-audit.md](maker-skills-verbatim-audit.md) for the complete change list. `check-styles.py` passes with zero drift. **Resolved 2026-08-21** — confirmed re-pasted live with the verbatim text at `/learn/create-course/maker-skills/mechanical` (see the confirmation note at the start of this row). Original 2026-08-15 note (superseded): New HTML embed (first conversion from Google Sites — one of the four Maker Skills sub-pages under Module 4, previously listed under "Not yet converted"; built by 4 parallel subagents alongside Electronics/Software/Wearables). Live URL slug confirmed as `mechanical` (the `Mechanical Making.md` snapshot's old `mechanical-making` slug was stale and has been corrected). **Known issue carried over from the live site, not introduced by this conversion:** the Matterport virtual tour link (`my.matterport.com/show/?m=rJtbANQgip3`) returns "Oops, model not available" — the 3D model appears to have been taken down; the live Google Sites page has the same dead link. **2026-08-19, site-wide "here"-link sweep (supersedes the 2026-08-19 typo-pass note below about leaving this alone):** CLAUDE.md's verbatim-conversion rule now explicitly carves out non-descriptive "here" links as an in-scope accessibility fix, not a wording change. Moved the Matterport link onto "virtual tour of the MIT Beaver Works space" and dropped the trailing "here"; wording otherwise unchanged. |
| [maker-skills-electronics.html](maker-skills-electronics.html) | [/learn/create-course/maker-skills/electronics](https://beaver-works-assistive-tech.mit.edu/learn/create-course/maker-skills/electronics) | 2026-08-21 | ✅ Synced | 2026-08-21 | **2026-08-21, confirmed pasted live (issue #25 reorg sync check):** Live-fetched the page's rendered HTML directly (same-origin `fetch` technique) — the new `/learn/create-course/maker-skills/electronics` URL is live with `/learn/create-course` module-nav links present. **2026-08-21 (issue #25 reorg link/copy sweep):** Live URL moved to `/learn/create-course/maker-skills/electronics`; module-nav links updated to `/learn/create-course/*`. **2026-08-19 correction:** same issue as `maker-skills-mechanical.html` above — reworded/condensed text (bullet lists replacing original prose, invented sub-headings not in the source) has been reverted to verbatim source wording; see [maker-skills-verbatim-audit.md](maker-skills-verbatim-audit.md). `check-styles.py` passes with zero drift. **Resolved 2026-08-21** — confirmed re-pasted live with the verbatim text at `/learn/create-course/maker-skills/electronics` (see the confirmation note at the start of this row). Original 2026-08-15 note (superseded): New HTML embed (first conversion from Google Sites — one of the four Maker Skills sub-pages under Module 4, previously listed under "Not yet converted"; built by 4 parallel subagents alongside Mechanical/Software/Wearables). No video embeds on this page (confirmed against the live page). All links checked live and working. |
| [maker-skills-software.html](maker-skills-software.html) | [/learn/create-course/maker-skills/software](https://beaver-works-assistive-tech.mit.edu/learn/create-course/maker-skills/software) | 2026-08-21 | ✅ Synced | 2026-08-21 | **2026-08-21, confirmed pasted live (issue #25 reorg sync check):** Live-fetched the page's rendered HTML directly (same-origin `fetch` technique) — the new `/learn/create-course/maker-skills/software` URL is live with `/learn/create-course` module-nav links present. **2026-08-21 (issue #25 reorg link/copy sweep):** Live URL moved to `/learn/create-course/maker-skills/software`; module-nav links updated to `/learn/create-course/*`. **2026-08-19 correction:** this was the worst-affected page — extensive rewording/condensing plus two real content fabrications (the "Web Development" TODO placeholders were replaced with invented finished-sounding prose; the App Inventor "About Us page" example sentence was dropped entirely). Full text reverted to verbatim source wording; see [maker-skills-verbatim-audit.md](maker-skills-verbatim-audit.md) for the complete section-by-section change list. `check-styles.py` passes with zero drift. **Resolved 2026-08-21** — confirmed re-pasted live with the verbatim text at `/learn/create-course/maker-skills/software` (see the confirmation note at the start of this row). Original 2026-08-15 note (superseded): New HTML embed (first conversion from Google Sites — the largest of the four Maker Skills sub-pages under Module 4, previously listed under "Not yet converted"; built by 4 parallel subagents alongside Mechanical/Electronics/Wearables). Content covers all 8 major sections: software project forms, Version Control (GitHub), UI/Front-End Development (Figma), Web Development, Desktop Software Development (packaging), Mobile Application Development (iOS/Android/MIT App Inventor), Software Accessibility Considerations, and AI/ML Tools for Accessibility (Machine Learning Lifecycle video, YouTube ID `ZmBUnJ7lGvQ`, Data Bias/Training Models subsections). **2026-08-19, pulled from `main` (commit `64e8caa`, "better structure accessibility principles") + follow-up fixes:** Hosea restructured the Software Accessibility Considerations section's 4 paragraphs into a `<ul>` list and, in the same commit, fixed the "here" link (Adobe color-contrast article) to use the article's real title as link text — this matches the site-wide "here"-link sweep done in this same session, so no further change was needed there. Two real bugs found while reviewing that commit and fixed locally just now: (1) the `randoma11y.com` link's `href` had been changed to a bare `randoma11y.com` (no `https://`), making it a broken relative link — restored to `https://www.randoma11y.com/`; (2) a stray empty `<p></p>` left after the new `<ul>` — removed. `check-styles.py` not yet re-run this pass. **Resolved 2026-08-21** — confirmed re-pasted live at `/learn/create-course/maker-skills/software` with the verbatim text, the accessibility-list restructure, and both link/markup fixes (see the confirmation note at the start of this row). |
| [maker-skills-wearables.html](maker-skills-wearables.html) | [/learn/create-course/maker-skills/wearables](https://beaver-works-assistive-tech.mit.edu/learn/create-course/maker-skills/wearables) | 2026-08-21 | ✅ Synced | 2026-08-21 | **2026-08-21, confirmed pasted live (issue #25 reorg sync check):** Live-fetched the page's rendered HTML directly (same-origin `fetch` technique) — the new `/learn/create-course/maker-skills/wearables` URL is live with `/learn/create-course` module-nav links present. **2026-08-21 (issue #25 reorg link/copy sweep):** Live URL moved to `/learn/create-course/maker-skills/wearables`; module-nav links updated to `/learn/create-course/*`. **2026-08-19 correction:** same issue as the other 3 Maker Skills sub-pages — reworded/condensed text (dropped sub-bullet detail, a silently "corrected" spelling of the source's own "Blufruit" typo, a reworded AFFOA paragraph) has been reverted to verbatim source wording, recovered from the still-live hidden `old-wearables` page (Hosea supplied the URL). See [maker-skills-verbatim-audit.md](maker-skills-verbatim-audit.md) for the full change list. `check-styles.py` passes with zero drift. **Resolved 2026-08-21** — confirmed re-pasted live with the verbatim text at `/learn/create-course/maker-skills/wearables` (see the confirmation note at the start of this row). Original 2026-08-15 note (superseded): New HTML embed (first conversion from Google Sites — one of the four Maker Skills sub-pages under Module 4, previously listed under "Not yet converted"; built by 4 parallel subagents alongside Mechanical/Electronics/Software). No video embeds on this page (confirmed against the live page). All links checked live and working. **2026-08-19, site-wide "here"-link sweep (supersedes the 2026-08-19 typo-pass note below about leaving this alone):** CLAUDE.md's verbatim-conversion rule now explicitly carves out non-descriptive "here" links as an in-scope accessibility fix, not a wording change. Moved the AFFOA curriculum link onto "curriculum" and dropped the trailing "here"; wording otherwise unchanged. |
| [maker-skills-artificial-intelligence.html](maker-skills-artificial-intelligence.html) | [/learn/create-course/maker-skills/artificial-intelligence](https://beaver-works-assistive-tech.mit.edu/learn/create-course/maker-skills/artificial-intelligence) | 2026-08-21 | ✅ Synced | 2026-08-21 | **2026-08-21, confirmed pasted live (issue #25 reorg sync check):** Live-fetched the page's rendered HTML directly (same-origin `fetch` technique) — the new `/learn/create-course/maker-skills/artificial-intelligence` URL is live with `/learn/create-course` module-nav links present. **2026-08-21 (issue #25 reorg link/copy sweep):** Live URL moved to `/learn/create-course/maker-skills/artificial-intelligence`; module-nav links updated to `/learn/create-course/*`. New HTML embed (fifth and final Maker Skills sub-page under Module 4, intentionally skipped in the 2026-08-15 batch pending its content image; converted now that the user added `website-images/ai-diagram.png`). Content is verbatim from the live page as of 2026-08-19: What Is AI, AI Is Not Just Chatbots (with the nested AI/ML/Deep Learning/Generative AI/LLM/Chatbots diagram and a "But what is a neural network?" video, YouTube `aircAruvnKk`), Large Language Models (a "Large Language Models explained briefly" video, YouTube `LPZh9BOjkQs`) and What Can LLMs Do, The Ethics of Large Language Models (Intellectual Property, Moderation and Training), Educational Risks, Privacy and Consent (with Local AI Models), AI and Disability, and the two "Your Team's Approach to AI as a Tool/Component" policy sections. Two live-page headings ("Intellectual Property", "Moderation and Training") were bold-styled text rather than real `<h>` elements on the source page — promoted to genuine `<h3>` for accessibility, matching their existing visual styling exactly. The two "Assignment: Draft an AI ... Policy" sections, which are their own top-level headings on the live page, were folded into `.reflect-box` callouts nested in their parent section, matching the reflect-box-for-assignments convention used elsewhere in the CRE[AT]E Course/Project pages rather than literally preserving the live page's flatter heading structure. A closing "Content Disclosure" section (present on this page only, not site-wide) was kept as its own section. `check-styles.py` passes with zero drift across all 27 pages; all external links spot-checked live except one CNBC article link that 403s to automated fetches (matches the live page's own link — likely bot-blocking, not a dead link). **Confirmed live 2026-08-19:** the page (like all others) references the diagram via `raw.githubusercontent.com/.../main/...`, which only resolves once the branch merges to `main` — that merge has since happened (PR #42), and fetching the live page's rendered HTML directly confirmed: the page exists live with a real page title, all major sections (What is AI, AI is not just chatbots, Large Language Models, Educational Risks, AI and Disability, both "Your Team's Approach" policy sections) are present, and the `ai-diagram.png` image itself returns HTTP 200. This row's earlier "Not yet pasted live" note was stale — it's already been pasted live and just wasn't recorded here. |
| [create-project.html](create-project.html) | [/learn/create-project](https://beaver-works-assistive-tech.mit.edu/learn/create-project) | 2026-08-21 | ✅ Synced | 2026-08-21 | **2026-08-21, confirmed pasted live (issue #25 reorg sync check):** Live-fetched the page's rendered HTML directly (same-origin `fetch` technique) — the new `/learn/create-project` URL is live with the updated `/current-teams/design-reviews` card link present, and the old nested design-reviews path is gone. **2026-08-21 (issue #25 reorg link/copy sweep):** Live URL moved to `/learn/create-project`; all 6 callout-card links updated, including Design Reviews now pointing to `/current-teams/design-reviews` (moved out of CRE[AT]E Project entirely). New HTML embed (first conversion from Google Sites — CRE[AT]E Project section index, previously listed under "Not yet converted"). Content was verbatim from the live page as of 2026-08-15: intro paragraph plus five callout cards (Finding a Co-Designer, Co-Designer Interviews, Project Brainstorming, Scoping a Project, Design Reviews), matching the live page's card set exactly at that time, including that "Simplify" did not get its own card there. All links are internal, no `target` attribute. Built as part of a 6-page parallel conversion of the whole CRE[AT]E Project section; `simplify.html` was intentionally skipped at that time — see its own row. **2026-08-19 update:** a sixth callout card, **Simplify**, was inserted between Scoping a Project and Design Reviews now that `simplify.html` exists. `check-styles.py` passes with zero drift across all 27 pages. **Confirmed live 2026-08-19 (second check, after user pasted the update):** fetched the live page's rendered HTML directly — all six cards' links (including `create-project/simplify`) are present. **Caveat found in the same check, resolved 2026-08-19:** the Design Reviews card links to `.../create-challenge/create-project/design-reviews` — briefly a dead URL after that page moved in the Google Sites hierarchy, now fixed (see the `design-reviews.html` row) and confirmed resolving correctly again. No local change was needed. |
| [finding-a-co-designer.html](finding-a-co-designer.html) | [/learn/create-project/finding-a-co-designer](https://beaver-works-assistive-tech.mit.edu/learn/create-project/finding-a-co-designer) | 2026-08-21 | ✅ Synced | 2026-08-21 | **2026-08-21, confirmed pasted live (issue #25 reorg sync check):** Live-fetched the page's rendered HTML directly (same-origin `fetch` technique) — the new `/learn/create-project/finding-a-co-designer` URL is live with the `/learn/create-project/co-designer-interviews` forward link present. **2026-08-21 (issue #25 reorg link/copy sweep):** Live URL moved to `/learn/create-project/finding-a-co-designer`; forward link updated to `/learn/create-project/co-designer-interviews`. New HTML embed (first conversion from Google Sites — first step of the CRE[AT]E Project section, previously listed under "Not yet converted"). Content ("Who can be a co-designer?" and "If you need to find a co-designer") is verbatim from the live page as of 2026-08-15, closing with a reflect-box "Assignment: Find a Co-Designer" that links forward to Co-Designer Interviews (internal, no `target`). No images on the live page. Part of the same 6-page parallel conversion noted on the `create-project.html` row above. `check-styles.py` passes with zero drift across all 26 pages. |
| [co-designer-interviews.html](co-designer-interviews.html) | [/learn/create-project/co-designer-interviews](https://beaver-works-assistive-tech.mit.edu/learn/create-project/co-designer-interviews) | 2026-08-21 | ✅ Synced | 2026-08-21 | **2026-08-21, confirmed pasted live (issue #25 reorg sync check):** Live-fetched the page's rendered HTML directly (same-origin `fetch` technique) — the new `/learn/create-project/co-designer-interviews` URL is live with `/learn/create-project` module-nav links present. **2026-08-21 (issue #25 reorg link/copy sweep):** Live URL moved to `/learn/create-project/co-designer-interviews`; Previous/Next module-nav links updated to `/learn/create-project/*`. New HTML embed (first conversion from Google Sites — second step of the CRE[AT]E Project section, previously listed under "Not yet converted"). Content — what a co-designer interview is, how to plan one, interviewing techniques and structure, what not to do, interview logistics, and interview protocols — is verbatim from the live page as of 2026-08-15, with two reflect-box assignments ("Interview Protocol" and "Needs and Requirements") and a closing module-nav interest band (Previous: Finding a Co-Designer / Next: Project Brainstorming, both internal links matching the live page's own Previous/Next text). No images on the live page. Part of the same 6-page parallel conversion noted on the `create-project.html` row above. `check-styles.py` passes with zero drift across all 26 pages. |
| [project-brainstorming.html](project-brainstorming.html) | [/learn/create-project/project-brainstorming](https://beaver-works-assistive-tech.mit.edu/learn/create-project/project-brainstorming) | 2026-08-21 | ✅ Synced | 2026-08-21 | **2026-08-21, confirmed pasted live (issue #25 reorg sync check):** Live-fetched the page's rendered HTML directly (same-origin `fetch` technique) — the new `/learn/create-project/project-brainstorming` URL is live with `/learn/create-project` module-nav links present. **2026-08-21 (issue #25 reorg link/copy sweep):** Live URL moved to `/learn/create-project/project-brainstorming`; inline and module-nav links updated to `/learn/create-project/*`. New HTML embed (first conversion from Google Sites — third step of the CRE[AT]E Project section, previously listed under "Not yet converted"). Content — brainstorming basics, the Five Whys/SCAMPER/Add Constraints/Individual Brainstorming methods, and External Search — is verbatim from the live page as of 2026-08-15, with reflect-box callouts for the Warning and the two Assignments ("Brainstorming Document" and "External Search"), plus a closing module-nav interest band (Previous: Co-Designer Interviews / Next: Scoping a Project, matching the live page's own Previous/Next text). No images on the live page. Part of the same 6-page parallel conversion noted on the `create-project.html` row above. `check-styles.py` passes with zero drift across all 26 pages. |
| [scoping-a-project.html](scoping-a-project.html) | [/learn/create-project/scoping-a-project](https://beaver-works-assistive-tech.mit.edu/learn/create-project/scoping-a-project) | 2026-08-21 | ✅ Synced | 2026-08-21 | **2026-08-21, confirmed pasted live (issue #25 reorg sync check):** Live-fetched the page's rendered HTML directly (same-origin `fetch` technique) — the new `/learn/create-project/scoping-a-project` URL is live with the `/learn/create-project/simplify` forward link present. **2026-08-21 (issue #25 reorg link/copy sweep):** Live URL moved to `/learn/create-project/scoping-a-project`; module-nav links updated (Next: Simplify now `/learn/create-project/simplify`). New HTML embed (first conversion from Google Sites — fourth step of the CRE[AT]E Project section, previously listed under "Not yet converted"). Content — the "Team exercise: Taking Stock" (Time/Funding/Expertise) reflect-box and "What we (generally) don't recommend" (including the safety-critical-products policy) — is verbatim from the live page as of 2026-08-15. No images on the live page. Part of the same 6-page parallel conversion noted on the `create-project.html` row above. **2026-08-19 update:** added a module-nav interest band (Previous: Project Brainstorming / Next: Simplify), now that `simplify.html` exists to link forward to. `check-styles.py` passes with zero drift across all 27 pages. **Confirmed live 2026-08-19 (second check, after user pasted the update):** fetched the live page's rendered HTML directly — links to both `project-brainstorming` and `create-project/simplify` are present in the nav band. |
| [simplify.html](simplify.html) | [/learn/create-project/simplify](https://beaver-works-assistive-tech.mit.edu/learn/create-project/simplify) | 2026-08-21 | ✅ Synced | 2026-08-21 | **2026-08-21, confirmed pasted live (issue #25 reorg sync check):** Live-fetched the page's rendered HTML directly (same-origin `fetch` technique) — the new `/learn/create-project/simplify` URL is live with the `/current-teams/design-reviews` forward link present. **2026-08-21 (issue #25 reorg link/copy sweep):** Live URL moved to `/learn/create-project/simplify`; "Next: Design Reviews" updated to `/current-teams/design-reviews`, and the design-document callout link updated to `/showcase/2024-projects-section/2024-project-page-vextraordinary`. New HTML embed (first conversion from Google Sites — the last unconverted page in the CRE[AT]E Project section, intentionally skipped in the 2026-08-15 batch conversion; see that batch's note below). Content — the "your project has failed" framing narrative and "Why simplify?" benefits list, the "Case Study: Chase's Watering Can" section (Team Vextraordinary, 2023–2024 season), and the "How can you simplify your design?" section with two assignments (Constrained Brainstorming, Red Teaming) — is verbatim from the live page as of 2026-08-19. **Embedded-document handling:** the live page's "Design Document: Chase's Watering Can" link is a Google Sites auto-generated rich link-preview card with a thumbnail image served from a signed, non-durable `lh3.googleusercontent.com` URL — not a stable asset this repo can depend on — so it was rebuilt as a standalone `.callout-card` (title, short body, arrow link) pointing to the same destination (`/create-challenge/2024-projects-section/2024-project-page-vextraordinary`) instead of reproducing the thumbnail. That destination page is itself not yet a local HTML embed (still native Google Sites) — link checked live and resolves correctly. Originally built with no Previous/Next module-nav interest band, matching the live page at that time (same as its siblings Scoping a Project and Design Reviews). **2026-08-19 update:** added a module-nav interest band (Previous: Scoping a Project / Next: Design Reviews); `create-project.html`, `scoping-a-project.html`, and `design-reviews.html` were updated in the same pass to cross-link to this page, completing the section's Previous/Next chain. `check-styles.py` passes with zero drift across all 27 pages. **Confirmed live 2026-08-19 (second check, after user created and pasted the page):** the page now exists live at the correct nested URL with correct title, and the "your project has failed" framing, the Chase's Watering Can case study, and the Previous/Next nav band are all present. **Caveat found in the same check, resolved 2026-08-19:** the "Next: Design Reviews →" button links to `.../create-challenge/create-project/design-reviews` — briefly a dead URL after that page moved in the Google Sites hierarchy, now fixed (see the `design-reviews.html` row) and confirmed resolving correctly again. No local change was needed. |
| [additional-resources.html](additional-resources.html) | [/current-teams/additional-resources](https://beaver-works-assistive-tech.mit.edu/current-teams/additional-resources) | 2026-08-21 | ✅ Synced | 2026-08-21 | **2026-08-21, confirmed pasted live (issue #25 reorg sync check):** Live-fetched the page's rendered HTML directly (same-origin `fetch` technique) — the new `/current-teams/additional-resources` URL is live, the `Students and Coaches` relabel is present, and the old `/create-challenge/create-course` link prefix is gone. **2026-08-21 (issue #25 reorg link/copy sweep):** Live URL moved to `/current-teams/additional-resources`; "Getting Started" heading/button text renamed to "Students and Coaches"; CRE[AT]E Course link updated to `/learn/create-course`. Style-guide drift fixes (body font-size, `.section__body` margin, callout-card spacing) and a new Interest Band section added before the footer (previously had none) — copy is placeholder-quality ("Ready to put these tools to use?" linking to Getting Started and the Course); revisit wording if it doesn't read right live. Then removed `target="_blank"` from 2 internal links (Getting Started, the Course). Pasted live and confirmed by user 2026-07-12. |
| [tips-for-teams.html](tips-for-teams.html) | [/current-teams/tips-for-teams](https://beaver-works-assistive-tech.mit.edu/current-teams/tips-for-teams) | 2026-08-21 | 🔄 Needs sync | 2026-08-21 | **2026-08-21, nav-flow fix:** page-nav `Back` link changed from "Goals of the Challenge" to "Students and Coaches" — Goals now sits earlier in the chain (About → Goals → Students and Coaches → Tips for Teams → …), so this page's immediate predecessor changed. `check-styles.py` passes with zero drift. **2026-08-21, confirmed pasted live (issue #25 reorg sync check):** Live-fetched the page's rendered HTML directly (same-origin `fetch` technique) — the new `/current-teams/tips-for-teams` URL is live with `/learn/create-course` and `/learn/create-project` links present. **2026-08-21 (issue #25 reorg link/copy sweep):** Live URL moved to `/current-teams/tips-for-teams`; CRE[AT]E Course/Project links updated to `/learn/create-course` and `/learn/create-project`. Style-guide drift fixes (`.section__list` spacing, `.section__ctas` margin, `.callouts`/`.callout-card` spacing). Pasted live and confirmed by user 2026-07-12. Already had no `target="_blank"` links — no further change needed. **2026-08-19 registration-link sweep:** the closing interest band still said "Team registration opens in September 2026" with a "Fill Out the Interest Form" button linking to the old interest-signup Google Form — updated to "Team registration ... is open now" with a "Register Your Team" button linking to the live formstack registration form (`create_team_application_2026_2027`, confirmed resolving live), now with `target="_blank" rel="noreferrer noopener"` as an external link. **Superseded same day by the registration-link simplification pass** — the button now points at `/create-challenge/current-forms` (internal, no `target`) instead of the direct formstack URL. **Confirmed live 2026-08-19:** fetched the live page's rendered HTML directly — "is open now" and "Register Your Team" linking to `current-forms` are both present. |
| [design-reviews.html](design-reviews.html) | [/current-teams/design-reviews](https://beaver-works-assistive-tech.mit.edu/current-teams/design-reviews) | 2026-08-21 | ✅ Synced | 2026-08-21 | **2026-08-21, confirmed pasted live (issue #25 reorg sync check):** Live-fetched the page's rendered HTML directly (same-origin `fetch` technique) — the new `/current-teams/design-reviews` URL is live with `/learn/create-course` and `/learn/create-project/simplify` links present. **2026-08-21 (issue #25 reorg link/copy sweep):** Live URL moved to `/current-teams/design-reviews` (page is now a Current Teams page, no longer nested under CRE[AT]E Project); Introduction/Design Processes/Documentation and Presentation/Idea to Ink links updated to `/learn/create-course/*`, and the "← Simplify" backlink to `/learn/create-project/simplify`. New HTML embed (first conversion from Google Sites — fifth/final step of the CRE[AT]E Project section, previously listed under "Not yet converted"; the largest page in the repo). Content — Peer/Remote Mentor Reviews, Time Zones/Extensions, all five design-review Gates (White/Orange/Blue/Brown/Black) each built as a native `<details>`/`<summary>` accordion per the live page's own "expand each section" note, and a 10-item Design Review FAQ built with the same `.faq-item` component as `faq.html` — is verbatim from the live page as of 2026-08-15, with one heading renamed for clarity ("Design Reviews" → "Remote Mentor Reviews" for the second Peer/Mentor subsection, which duplicated the page title on the live site) and four stale/broken internal links corrected to their current URLs (Introduction, Design Processes, Documentation and Presentation, and the FAQ's Idea to Ink link — all previously pointed at pre-conversion "old-..." slugs or a dead self-referential anchor on the live site). No images on the live page. Part of the same 6-page parallel conversion noted on the `create-project.html` row above. **2026-08-19 update:** added a module-nav interest band with a single Previous: Simplify link (no Next — still the last step of the section), now that `simplify.html` exists. `check-styles.py` passes with zero drift across all 27 pages. **2026-08-19, first re-check after that update — found a URL/site-structure problem, not a simple sync gap:** the page had been moved up one level in the Google Sites page hierarchy (out from under CRE[AT]E Project), so `/create-challenge/create-project/design-reviews` hard-404'd live while the content actually lived at `/create-challenge/design-reviews`. This broke 4 internal links elsewhere in the repo that point at the nested path. **2026-08-19, second re-check — confirmed fixed:** the user moved the page back into its nested location in Google Sites. Fetching `/create-challenge/create-project/design-reviews` directly now returns the correct content (Gates, Peer review sections, FAQ, and the "Coming from Simplify?" nav band all present, no 404), and the flat `/create-challenge/design-reviews` URL now 404s as expected — the page lives in one place again, matching its siblings and every existing internal link. The 4 dependent links in `schedule.html`, `create-project.html`, `documentation-and-presentation.html`, and `simplify.html` needed no changes and now resolve correctly. **2026-08-19 registration-link sweep, follow-up:** the media release form link in the design-review FAQ (previously flagged in that sweep as "out of scope, not fixed" since I didn't know its current formstack slug) has been fixed a different way — per Hosea's instruction, it now points to the site's `/create-challenge/current-forms` page instead of a direct formstack URL, matching the identical media-release-form link two paragraphs earlier in the same FAQ (which already used that pattern). This avoids the direct-link staleness problem entirely: the old link was `mit-bwsi.formstack.com/forms/create_codesigner_media_release_24_25` (a year-old slug); the Current Forms page is confirmed live (title "Beaver Works AT - Current Forms") and is the same indirection `getting-started.html`-style pages should probably use for any form link likely to change yearly. Also dropped `target="_blank"` since it's now an internal link, per the corrected link-target rule in CLAUDE.md. **Confirmed live 2026-08-19:** fetched the live page's rendered HTML directly — no `create_codesigner_media_release_24_25` slug remains, `current-forms` links are present, and the Simplify Previous-nav link is present. **2026-08-19, site-wide "here"-link sweep:** the FAQ's media-release-form sentence used the literal link text "here" — non-descriptive per CLAUDE.md's accessibility rules and explicitly in-scope for a fix-and-log correction. Moved the link onto "media release form" (its first mention in the same sentence) and dropped the trailing "here"; wording otherwise unchanged. **Resolved 2026-08-21** — confirmed re-pasted live at the new `/current-teams/design-reviews` URL with the media-release-form link-text fix (see the confirmation note at the start of this row). |
| [2026-projects.html](2026-projects.html) | [/showcase/2026-projects](https://beaver-works-assistive-tech.mit.edu/showcase/2026-projects) | 2026-08-21 | ✅ Synced | 2026-08-21 | **2026-08-21, confirmed pasted live (issue #25 reorg sync check):** Live-fetched the page's rendered HTML directly (same-origin `fetch` technique) — the individual 2023/2024/2025 Projects links are live and the bare `/past-projects` link is gone. **2026-08-21 (issue #25 reorg link/copy sweep):** Live URL moved to `/showcase/2026-projects`; the "All past projects" button (which linked to a bare `/past-projects` that 404s under both schemes) was replaced with individual 2023/2024/2025 Projects links, matching the pattern already used on `index.html`. Removed stray `'DM Sans'` from body font-family, added missing `<meta name="description">`, then removed `target="_blank"` from 2 internal links (Past Projects index, Getting Started). Pasted live and confirmed by user 2026-07-12. |

## Local drafts not yet linked to a snapshot

These HTML files exist in the repo but their `site-snapshot` entry has no `source_html` field and is still marked `content_status: stub` — meaning it's unclear whether this embed was ever pushed to the live site at all. Confirm live status before relying on the sync table above for these.

| Local file | Live URL | Last local edit | Notes |
|---|---|---|---|
| [goals-of-the-challenge.html](goals-of-the-challenge.html) | [/create-challenge/goals-of-the-challenge](https://beaver-works-assistive-tech.mit.edu/create-challenge/goals-of-the-challenge) | 2026-08-21 | 🔄 **Needs sync** (see note) — **2026-08-21, nav-flow fix:** page-nav changed from `Back: Students and Coaches` / `Next: Tips for Teams` to `Back: About` / `Next: Students and Coaches`, restoring the intended About → Goals of the Challenge → Students and Coaches reading order (the reorg had it running backwards, with Goals disconnected from About entirely). Left the "Register / About the Challenge" boilerplate band untouched — that's a standardized CTA reused verbatim across other pages, not a chain pointer. `check-styles.py` passes with zero drift. **2026-08-21, confirmed pasted live (issue #25 reorg sync check):** Live-fetched the page's rendered HTML directly — the `Students and Coaches` backlink relabel is live. **2026-08-21 (issue #25 reorg link/copy sweep):** "Getting Started" backlink label renamed to "Students and Coaches" (href unaffected — page already pointed at the new URL). Wrapped the "most important learning objective" sentence in `<strong>` in the Learning Objectives section (2026-08-15). Checked live 2026-08-15: the `<strong>`-wrapped sentence matches the local file exactly — ✅ **already pasted live**, the earlier "not yet pasted" note was stale. `site-snapshot` entry still needs its `source_html` field added per the workflow in CLAUDE.md (unrelated to sync status). Re-confirmed live 2026-08-19 (fetched rendered HTML; the sentence is still wrapped in `<strong>`). **2026-08-19 registration-link sweep:** the closing interest band still said "Team registration opens in September 2026" with a "Fill Out the Interest Form" button linking to the old interest-signup Google Form, despite its own heading already saying "Register for the 2026–2027 season" — updated the body copy and button to match, now linking to the live formstack registration form (`create_team_application_2026_2027`, confirmed resolving live) with `target="_blank" rel="noreferrer noopener"`. **Superseded same day by the registration-link simplification pass** — the button now points at `/create-challenge/current-forms` (internal, no `target`) instead of the direct formstack URL. **Confirmed live 2026-08-19:** fetched the live page's rendered HTML directly — `current-forms` links are present, no direct formstack link remains. Pasted live. |

## Not yet converted to an HTML embed

These pages are still native Google Sites content with no local HTML embed. Not "out of sync" — just not started. Listed for backlog visibility only; move a page out of this list and into the table above once it gets a local HTML embed.

Nav locations below reflect the live site as of the 2026-08-21 reorg (issue #25) — see that dated entry near the bottom of this file for the full audit.

- CRE[AT]E Challenge: Registration Process, Current Forms, Final Event (2023, 2024, 2025, 2026)
- Take Part: Co-Designers, Partner Organizations — **new pages, not yet built at all; see the follow-up comment on issue #25**
- Current Teams: Challenge Dashboard, Alumni Division
- Showcase: 2023, 2024, 2025 Projects (2026 is already an embed — see table above)
- Previous Programs: Design for Education Aids (Crash Course, Projects), Design of AT (2021 Documentation, 2022 Assignments, 2022 Documentation, About Design of AT, Final Prototype, Initial Prototype, Product Requirements)

---

*Last reviewed: 2026-08-21, issue #25 reorg sync check confirmed live — every row in the table above is ✅ Synced (see the dated entry near the bottom of this file for the full audit). Previous review, 2026-08-19: registration-link simplification confirmed live (see note near the bottom of this file). Earlier same-day review: full-site sync audit (see note below). All 27 pages pass `check-styles.py` with zero drift against `reference-style.css`. This review checked every row previously marked 🔄 Needs sync (`maker-skills.html`, `faq.html`, `idea-to-ink.html`, `goals-of-the-challenge.html`) directly against the live Google Site by inspecting the rendered embed content (the Google Sites embed is a closed-shadow-DOM component, so content was extracted from the page's hydration HTML rather than a visual screenshot) — all four matched their local files exactly and their status was stale; they're now marked ✅ Synced. `design-processes.html` (merged via PR #36, "Add Design Processes page as local HTML embed") was found to have no row in this table at all despite already being live; it's now added, and two real bugs found live were fixed locally (internal module-nav links had `target="_blank"`, and `.reflect-box` had a 24px/32px margin-top drift) — this row was 🔄 Needs sync until those fixes were pasted live (now resolved, see below). `documentation-and-presentation.html` (Module 5 of the CRE[AT]E Course) was converted from Google Sites to a local HTML embed for the first time — see its row above; it was 🔄 Needs sync until pasted live (now resolved, see below).

**2026-08-15 Maker Skills conversion pass:** the four Maker Skills sub-pages (Mechanical, Electronics, Software, Wearables — Module 4 of the CRE[AT]E Course) were converted from Google Sites to local HTML embeds for the first time via 4 parallel subagents, each scoped to touch only its own new HTML file and its own `site-snapshot` entry to avoid write conflicts; see their rows above — all four are 🔄 Needs sync until pasted live. Content and URL slugs were confirmed directly against the live Google Site (the Mechanical Making snapshot's `mechanical-making` URL slug was found stale and corrected to `mechanical`), and every external link across the four pages was checked live: two pre-existing dead links were found and left as-is (matching the live site) rather than silently altered — the Matterport virtual tour on the Mechanical page ("model not available") and `randoma11y.com` on the Software page (invalid SSL certificate) — see their table rows for details. The Artificial Intelligence Maker Skills sub-page was intentionally left unconverted and added to "Not yet converted" below.

**2026-08-15 CRE[AT]E Project conversion pass:** the entire CRE[AT]E Project section (minus `Simplify`, which has a content image and was intentionally skipped) was converted from Google Sites to local HTML embeds in one parallel pass by six subagents, each independently reading the live page, building the HTML per the site style guide, updating its own `site-snapshot` entry, and running `check-styles.py` — `create-project.html` (section index), `finding-a-co-designer.html`, `co-designer-interviews.html`, `project-brainstorming.html`, `scoping-a-project.html`, and `design-reviews.html`. All six are new rows above, and all six pages' cross-links to each other were spot-checked afterward for correctness (index → all five sub-pages; each sub-page's Previous/Next module nav) and confirmed to point at the right internal URLs with no `target="_top"` anywhere in the repo. `design-reviews.html` additionally corrects four stale internal links on the live Google Site (see its row above) and introduces one new page-specific component (a `<details>`/`<summary>` accordion for the five design-review Gates) documented inline in its `<style>` block. This batch was confirmed pushed and ✅ Synced live as of 2026-08-19 (four of the six rows — `finding-a-co-designer.html`, `co-designer-interviews.html`, `project-brainstorming.html`, plus the four Maker Skills sub-pages from the same day's other batch — remain in that state; `create-project.html`, `scoping-a-project.html`, and `design-reviews.html` have since had further local-only edits, see below).

**2026-08-19 Simplify conversion:** `simplify.html`, the one page skipped in the 2026-08-15 CRE[AT]E Project batch, was converted — see its row above and its updated `site-snapshot` entry for details on how the live page's Google-Sites-native rich link-preview card (the "embedded document") was rebuilt as a `.callout-card` rather than reproducing its non-durable thumbnail image URL. This completes the CRE[AT]E Project section: all six pages now have local HTML embeds.

**2026-08-19 CRE[AT]E Project cross-linking:** with `simplify.html` now built, `create-project.html` (added a sixth callout card), `scoping-a-project.html`, and `design-reviews.html` (both gained their first-ever module-nav interest band) were updated to link it into the section's Previous/Next sequence — Scoping a Project → Simplify → Design Reviews — and `simplify.html` itself got matching Previous/Next links. All three rows flip back to 🔄 Needs sync as a result, even though their base content was independently confirmed live earlier the same day (see their rows above). `check-styles.py` passes with zero drift across all 27 pages.

**2026-08-19 Schedule update:** added a Simplify due-date row (February 21) to `schedule.html`'s Spring Semester table, between the Orange Gate / Preliminary Design Review (Feb 7) and Maker Skills Section (Feb 28) rows — see the `schedule.html` row above.

**2026-08-19 Maker Skills — Artificial Intelligence:** the fifth Maker Skills sub-page, intentionally skipped in the 2026-08-15 batch because its content image wasn't available yet, was converted now that the user added `website-images/ai-diagram.png`. See its row above.

**2026-08-19 full-site sync audit:** every row in this file was re-checked against the live site. Google Sites' embed content isn't exposed to normal DOM inspection or `WebFetch` (closed shadow DOM), so verification used a same-origin `fetch(window.location.href)` against each live page's own rendered HTML (via the Browser pane's `javascript_tool`) and searched it for specific section headings, link hrefs, video IDs, and other markers pulled from each local file — a more reliable technique than the "hydration HTML" screen-scraping used in earlier passes, and one worth reusing next time. Six Haiku subagents were run in parallel first to cover ground quickly; their results were uneven (a few gave real live-verified findings, several instead just reasoned from this file's own prior notes or compared against the stale `site-snapshot/` archive, which produced misleading "drift" findings against the wrong reference) — all findings below were independently re-confirmed directly against the live site before being recorded here. Findings:
- **Corrected several self-contradictory rows:** `design-processes.html`, `documentation-and-presentation.html`, and all four Maker Skills sub-pages (`maker-skills-mechanical.html`, `maker-skills-electronics.html`, `maker-skills-software.html`, `maker-skills-wearables.html`) were marked ✅ Synced in the status column but their own notes said "Not yet pasted live" — a stale-note bug, not a real sync gap. Live-fetched each one directly: all six are genuinely live and match their local files. Notes corrected; status column was already right.
- **`maker-skills-artificial-intelligence.html` flipped from 🔄 Needs sync to ✅ Synced:** it was recorded as blocked on the `ai-diagram.png` merge to `main`, but that merge (PR #42) has since happened, and a live fetch confirms the page, all its sections, and the diagram image (HTTP 200) are already live.
- **Confirmed still 🔄 Needs sync, with direct live evidence this time** (previously inferred from local edit history, not verified): `index.html`, `schedule.html`, `create-project.html`, `scoping-a-project.html`, `design-reviews.html`. `simplify.html` in particular 404s live — the page doesn't exist yet, so it can't be pasted into until it's created in the Google Sites site map first.
- **Spot-checked and confirmed accurate, no drift:** `about.html`, `maker-skills.html`, `faq.html`, `idea-to-ink.html`, `2026-projects.html` (team-number set matches exactly, 42 unique team numbers both sides), `goals-of-the-challenge.html`.
- **New non-sync finding:** `technical-mentors.html`'s footer reads "© 2025 MIT Beaver Works" on both the local file and the live page (confirmed identical) — a real content bug, but not a sync gap since both copies agree. Worth a follow-up fix.
- `finding-a-co-designer.html`, `co-designer-interviews.html`, `project-brainstorming.html`, `additional-resources.html`, `create-course.html`, `course-introduction.html`, `tips-for-teams.html`, and `getting-started.html` were **not** independently re-verified this pass (no local edits since their last confirmed sync, and no contradiction flags) — treat their ✅ Synced status as carried forward rather than freshly confirmed.

**2026-08-19 second-pass re-check (same day, after the user pasted updates live):** re-verified the 6 pages still marked 🔄 Needs sync from the pass above, plus the `technical-mentors.html` footer-year finding, using the same live-fetch technique.
- **Confirmed resolved, flipped to ✅ Synced:** `index.html`, `schedule.html`, `create-project.html`, `scoping-a-project.html`, `simplify.html` — all five now have their pending local changes live and verified (testimonials/registration messaging, the Simplify schedule row, the sixth callout card, the nav bands, and the Simplify page itself all present and correct).
- **`technical-mentors.html` footer-year bug: confirmed fixed** — local file and live page both now read "© 2026" (commit `e30cb84`).
- **`design-reviews.html`: initially NOT resolved, and a different problem than "needs sync."** The old URL (`/create-challenge/create-project/design-reviews`) hard-404'd live, reproduced on two fresh page loads. The site's own nav link for "Design Reviews" pointed to `/create-challenge/design-reviews` — the page had been moved up one level in the Google Sites page tree during editing, out from under CRE[AT]E Project, unlike its five sibling pages which stayed correctly nested. The content itself, fetched at that URL, was fully correct and up to date (including the new nav band) — so it wasn't a content/sync issue, it was a site-structure issue. It also broke 4 internal links elsewhere in the repo that pointed at the old nested path: 5 links in `schedule.html`, 1 in `create-project.html`, 1 in `documentation-and-presentation.html`, and 1 in `simplify.html`. Flagged to Hosea rather than guessed at, since there were two valid fixes (move the page back, or flatten every link to match). **Resolved 2026-08-19, third re-check:** Hosea moved the page back into its nested location in Google Sites. Re-verified directly: `/create-challenge/create-project/design-reviews` now returns the correct content with no 404, the flat `/create-challenge/design-reviews` URL now 404s as expected, and all 4 dependent links elsewhere in the repo resolve correctly again with no local changes needed. `design-reviews.html` flipped to ✅ Synced — this closes out the last open row from this audit; all 27 pages are now ✅ Synced.

**2026-08-19 registration-link sweep:** checked every registration/notify link site-wide against the live formstack registration form (`https://mit-bwsi.formstack.com/forms/create_team_application_2026_2027`, confirmed resolving live) already in use on `index.html` and `getting-started.html`. Found and fixed two categories of stale link, all on pages that predated registration opening and still carried "registration opens in September" / "get notified" / "interest form" language from before the season started:
- **Old interest-signup Google Form** (`docs.google.com/forms/d/e/1FAIpQLSfjykFOIogTkRrczyDZ8J1PyDbEiwDTHsGh3cNt8zFXnhzZhw/...`) — replaced with the live registration form, and surrounding copy updated from "opens in September" / "get notified" to "is open now" / "register", on `about.html` (2 spots), `goals-of-the-challenge.html`, `tips-for-teams.html`, and `schedule.html`'s closing interest band.
- **Stale/malformed formstack slug** on `schedule.html`'s Fall Semester table — `create_teamapplication_2024_2025` (two seasons old, and missing the underscore between "team" and "application" that the current slug uses) — corrected to `create_team_application_2026_2027`.
- All updated links now use `target="_blank" rel="noreferrer noopener"` per the external-link convention, and were spot-checked live (see individual rows above). **Superseded same day by the registration-link simplification pass below** — these direct-formstack links were replaced with `/create-challenge/current-forms` before ever going live, so this intermediate state was never pasted live at all.
- **Out of scope, flagged not fixed:** separately, the live formstack registration form's own page header text still reads "Application is due October 5th, 2025 / kickoff Saturday October 18th, 2025" — stale copy on Formstack's side, outside this repo, worth flagging to whoever manages that form.

**2026-08-19 media release form fix (follow-up to the sweep above):** `design-reviews.html`'s media release form link (`create_codesigner_media_release_24_25`, a year-old slug — flagged above as "out of scope, not fixed" since the current slug wasn't known) was instead pointed at `/create-challenge/current-forms`, matching an identical media-release-form link two paragraphs earlier in the same FAQ that already used that pattern. Confirmed the Current Forms page resolves live (title "Beaver Works AT - Current Forms").

**2026-08-19 registration-link simplification (supersedes the direct-formstack-link fixes above):** per Hosea's request to simplify further, every registration link site-wide — including the ones fixed earlier the same day to point directly at `mit-bwsi.formstack.com/forms/create_team_application_2026_2027` — was switched to point at `/create-challenge/current-forms` instead, using that page as a single outbound-redirect hub rather than hardcoding the formstack slug in multiple places. This means the site never needs a repo-wide link sweep again when the registration form's URL changes year to year — only the Current Forms page itself needs updating. Affected: `index.html` (status line + interest band), `about.html` (2 spots), `getting-started.html` (2 spots), `goals-of-the-challenge.html`, `tips-for-teams.html`, and `schedule.html` (Fall Semester table row + interest band) — 10 links across 6 pages, plus the 2 already pointing at Current Forms in `design-reviews.html`. All dropped `target="_blank" rel="noreferrer noopener"` since the destination is now internal, per CLAUDE.md's link-target rule. `check-styles.py` passes with zero drift across all 28 pages. **Trade-off worth flagging:** the Current Forms page lists multiple forms (registration, design review submission, final report, co-designer consent) rather than jumping straight to the registration form, so these CTAs — including the homepage's primary "Register your team" buttons — now take visitors to a list instead of the form directly.

**2026-08-19 registration-link simplification: confirmed pasted live (final check).** Re-checked all 7 affected pages (`index.html`, `about.html`, `getting-started.html`, `goals-of-the-challenge.html`, `tips-for-teams.html`, `schedule.html`, plus `design-reviews.html`'s already-current-forms media-release links) by fetching each live page's rendered HTML directly and confirming: `/create-challenge/current-forms` links are present, no direct `mit-bwsi.formstack.com/forms/create_team_application_2026_2027` link remains anywhere, no old interest-signup Google Form link remains, and no stale `create_teamapplication_2024_2025` slug remains on `schedule.html`. All 7 rows flipped to ✅ Synced above — every row in this file is now ✅ Synced.

**2026-08-19 Maker Skills typo/formatting fixes (follow-up to the verbatim correction below):** with the source text restored verbatim, Hosea asked to then go fix the genuine misspellings/formatting errors that were present in the *original* source (previously deliberately left as-is during the verbatim pass). Fixed on `maker-skills-software.html`: two duplicated-word typos ("the the use of," "development development"), one wrong-word typo ("willing the pay" → "to pay"), one grammar fix ("help with decide" → "help decide"), one stray space before a period, "Beagle Bone" → "BeagleBone," and "Objective C" → "Objective-C". Fixed on `maker-skills-electronics.html`: "Sparkfun" → "SparkFun" (2 spots, correct brand capitalization). Fixed on `maker-skills-wearables.html`: "Blufruit" → "Bluefruit" (correct Adafruit product line name). Two non-descriptive "here" links (Mechanical's Matterport tour, Wearables' AFFOA curriculum) were deliberately left alone — fixing those means adding descriptive wording, not correcting an objective error — see [maker-skills-verbatim-audit.md](maker-skills-verbatim-audit.md) for the full list and that judgment call. `check-styles.py` passes with zero drift on all 4 pages. All 4 pages remain 🔄 Needs sync (unchanged from below) since none of this has been pasted live yet.

**2026-08-19 Maker Skills verbatim-text correction:** Hosea found that the 2026-08-15 Maker Skills conversions (Mechanical, Electronics, Software, Wearables) had been substantially reworded/condensed from the source Google Sites pages rather than transcribed verbatim, contradicting this file's own "verbatim from the live page" claims. Source text was recovered from the still-live hidden pages (`old-mechanical`, `old-electronics`, `old-software`, `old-wearables` — renamed and unlinked from nav, but not deleted; Hosea supplied the `old-wearables` URL directly after my own guesses at it all 404'd) and all four pages were rewritten to restore the original wording exactly, restructured only where needed to fit the site's HTML components. Full section-by-section change list, including several real content fabrications/silent corrections that were caught and reverted (an invented Onshape "collaboration" claim on Mechanical; invented finished-sounding prose replacing literal `TODO` placeholders on Software, plus a dropped App Inventor example sentence; the source's own "Blufruit" typo silently "corrected" to "Bluefruit" on Wearables), is in [maker-skills-verbatim-audit.md](maker-skills-verbatim-audit.md). `check-styles.py` passes with zero drift on all four. **The Maker Skills index page was not checked** — no hidden "old" version could be found at guessed URLs; still need Hosea to confirm that URL (or confirm no old version exists) before the same pass can run on it. See [CLAUDE.md](CLAUDE.md)'s updated Google Sites to HTML Embed Workflow for the new verbatim-text rule this should have caught the first time.

**2026-08-19 site-wide "here"-link accessibility sweep:** searched every page in the repo for non-descriptive link text (`here`, `click here`, `this link`, `read more`, `learn more`) using a regex over all anchor tags, not just a plain-text search, to catch whitespace/casing variants. Found 4 live instances beyond the one already fixed on `maker-skills-software.html` (pulled from `main`, see that row above): `design-reviews.html` (media release form), `faq.html` (Edly help guide), `maker-skills-mechanical.html` (Matterport virtual tour), and `maker-skills-wearables.html` (AFFOA curriculum). All fixed the same way — moved the link onto the nearest already-present descriptive noun phrase in the same sentence and dropped the trailing "here," with no other wording changes. This supersedes the 2026-08-19 Maker Skills typo-pass note (see below) that had deliberately left the Mechanical and Wearables "here" links alone, since CLAUDE.md's verbatim-conversion rule now explicitly carves out this exact fix as in-scope. `check-styles.py` passes with zero drift across all 28 pages. Re-ran the same regex after the fixes — zero remaining matches site-wide. Four rows flip to 🔄 Needs sync as a result (`design-reviews.html`, `faq.html`; `maker-skills-mechanical.html` and `maker-skills-wearables.html` were already 🔄 Needs sync from the earlier verbatim-correction pass).

**2026-08-21 reorg link/copy sweep (issue #25):** Hosea reorganized the site's navigation into audience-based top-level sections (Home / CRE[AT]E Challenge / Take Part / Learn / Current Teams / Showcase / Donate, replacing the old flat CRE[AT]E-Challenge-only menu) directly on the live Google Site. This moved most pages to new URLs without touching this repo, so every local embed's hardcoded internal `href`s pointed at the old, now-404ing paths. Confirmed the new nav tree by fetching each live page's own rendered HTML (`fetch(window.location.href)` via the Browser pane, same technique as the 2026-08-19 full-site sync audit above — Google Sites' nav is closed-shadow-DOM) and extracting the `data-navtype`/`data-level` link entries; posted the full audit as a [comment on issue #25](https://github.com/hoseasiu/create-challenge-website/issues/25) before making any changes.

URL prefix mapping applied across ~24 files (~70 links):
- `/create-challenge/create-course/*` → `/learn/create-course/*`
- `/create-challenge/create-project/{finding-a-co-designer,co-designer-interviews,project-brainstorming,scoping-a-project,simplify}` → `/learn/create-project/*`
- `/create-challenge/create-project/design-reviews` → `/current-teams/design-reviews` (page moved out of CRE[AT]E Project into Current Teams, standing alone rather than a prefix swap)
- `/create-challenge/technical-mentors` → `/take-part/technical-mentors`
- `/create-challenge/tips-for-teams` → `/current-teams/tips-for-teams`
- `/create-challenge/getting-started` → `/take-part/students-and-coaches` (page renamed, not just moved — see below)
- `/additional-resources` → `/current-teams/additional-resources`
- `/past-projects/{year}-projects` → `/showcase/{year}-projects`, and `/create-challenge/{year}-projects-section/...` → `/showcase/{year}-projects-section/...`
- Unaffected: `/home`, `/create-challenge/about`, `/create-challenge/goals-of-the-challenge`, `/create-challenge/schedule`, `/create-challenge/faq`, `/create-challenge/current-forms`, `/create-challenge/registration-process`

**"Getting Started" → "Students and Coaches":** the live page's nav label and title changed, so `getting-started.html` was renamed to `students-and-coaches.html` (git history preserved via `git mv`), its `<title>` and `sr-only` `<h1>` updated to match, and its `site-snapshot` entry renamed/updated (`source_html: students-and-coaches.html`) — see its new row above, moved up from "Local drafts not yet linked to a snapshot" now that it has a `source_html` field. Every other page linking to it with "Getting Started" as the visible text — `about.html` (×2), `additional-resources.html` (×2, heading + button), `goals-of-the-challenge.html`, `index.html` — was updated to say "Students and Coaches" instead. Page content itself is unchanged; whether it should be rewritten for the Students-and-Coaches audience framing (rather than just relabeled) is deferred to a separate follow-up issue per Hosea's instruction.

**Adjacent bugs fixed in the same pass** (pre-existing, not caused by the reorg, but touching files already being edited): `create-course.html`'s Maker Skills section was missing the Artificial Intelligence card that exists on `maker-skills.html` and in the live nav — added it (same card style, body copy written to match the page's existing four cards) and corrected "four areas" to "five areas"; also fixed the long-stale `maker-skills/mechanical-making` slug (should always have been `maker-skills/mechanical`) on the Mechanical Making card. `index.html`'s Students and Coaches button dropped `target="_blank" rel="noreferrer noopener"`, now that it's an internal link, per CLAUDE.md's corrected link-target rule. `2026-projects.html`'s "All past projects" button linked to a bare `/past-projects` that 404s under both the old and new URL scheme (no such index page exists) — replaced with individual 2023/2024/2025 Projects links, matching the pattern `index.html` already uses (Hosea's choice among the options presented).

**Verification:** extracted every internal `href` across all 28 HTML files (`grep -hoE 'href="https://beaver-works-assistive-tech\.mit\.edu[^"]*"'`) and live-fetched each of the 31 unique destination URLs — all return HTTP 200. `check-styles.py` passes with zero drift across all 28 pages. Every row whose live URL or page content changed in this pass is flagged 🔄 Needs sync above; nothing has been re-pasted live yet.

**Deferred, per Hosea's instruction — not built in this pass:** new pages implied by the reorg (Take Part: Co-Designers, Partner Organizations; a real Showcase/past-projects index; Current Teams: Forms & Deadlines, Project Funding) are net-new content, not link fixes — see the follow-up comment on issue #25 for the full list, and a separate new issue for the `students-and-coaches.html` content-framing question above.

**2026-08-21, issue #25 reorg sync check (confirmed pasted live):** Hosea reported having pasted all 27 updated embeds from the reorg pass above into their new/moved Google Sites embed blocks. Verified this directly rather than taking it on faith: live-fetched every affected page's own rendered HTML (same-origin `fetch(base + url)` run from the Browser pane against all 28 URLs in parallel, same technique as the 2026-08-19 full-site sync audit) and checked each one for (a) HTTP 200 at its new post-reorg URL and (b) a content marker specific to that page's reorg change — a renamed link, an added card, a corrected slug, or a moved link prefix. All 28 pages passed on the first pass. Ran a second targeted pass checking that stale pre-reorg paths are actually *gone* (not just that new content is present) — confirmed `/create-challenge/getting-started`, the old nested `/create-challenge/create-project/design-reviews`, the bare `/past-projects` link, the stale `mechanical-making` slug, the old `/create-challenge/create-course` prefix, and `target="_blank"` on the `index.html` Students and Coaches link are all absent from the live pages that used to carry them. Every row in the main table above (and the `goals-of-the-challenge.html` row in "Local drafts not yet linked to a snapshot") is now ✅ Synced with "Last synced" set to 2026-08-21; several rows also had stale in-note "needs to be re-pasted live" clauses left over from before this check (`faq.html`, `students-and-coaches.html`, `design-processes.html`, `idea-to-ink.html`, all four Maker Skills sub-pages, `design-reviews.html`) that have been corrected to reflect the now-confirmed live status, matching the self-contradictory-row cleanup pattern from the 2026-08-19 audit. **Not independently re-verified this pass** (no reorg-related change, and no prior contradiction flag): none — every row that changed in the PR #47 diff was covered.

**2026-08-21, pages-list reorder:** the main table above and the "Not yet converted" backlog list were reordered to follow the live site's audience-based nav structure (Home → CRE[AT]E Challenge → Take Part → Learn → Current Teams → Showcase), replacing the previous ad-hoc/historical ordering. Within "Learn," pages follow the CRE[AT]E Course module sequence (Introduction → Design Processes → Idea to Ink → Documentation and Presentation → Maker Skills index → its five sub-pages → CRE[AT]E Project index → its five sub-pages). This is a pure reorder — no row's content, status, or notes changed as a result (aside from the sync-check updates in the entry above, done in the same pass). The "Local drafts not yet linked to a snapshot" and "Not yet converted" sections keep their existing table/list structure since reordering them further wouldn't add clarity (the former has one row; the latter's items were already grouped by nav section, just not in nav order — now fixed to match).

Update this file whenever a local HTML embed changes or whenever a push to the live site is confirmed — see the workflow in [CLAUDE.md](CLAUDE.md).*
